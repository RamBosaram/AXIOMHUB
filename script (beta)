--[[
    AXIOM 2.0 — MM2 Suite
    Box widgets with shape selector + float toggle
    Per-role ESP boxes, maximalist space background
]]

if not game:IsLoaded() then
    game:GetService("StarterGui"):SetCore("SendNotification", {Title = "AXIOM", Text = "Loading...", Duration = 5})
    game.Loaded:Wait()
end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

----------------------------------------------------------------
-- CONFIG
----------------------------------------------------------------
local CONFIG_FILE = "AXIOM_MM2_config.json"
local BLACKLIST_KEYS = {
    hubOpen = true, fly = true, noclip = true, touchFling = true,
    autoShoot = true, autoKnifeThrow = true, killAuraOn = true,
    silentThrowAim = true, invisible = true, infJump = true,
    streamProof = true, aiState = true,
}

local ConfigManager = {}
ConfigManager.loaded = {}
ConfigManager.saveTask = nil

function ConfigManager:load()
    if not readfile or not isfile then return end
    if not isfile(CONFIG_FILE) then return end
    local ok, content = pcall(readfile, CONFIG_FILE)
    if not ok or not content then return end
    local ok2, data = pcall(function() return HttpService:JSONDecode(content) end)
    if not ok2 or type(data) ~= "table" then return end
    self.loaded = data
end

function ConfigManager:apply(t)
    for k, v in pairs(self.loaded) do
        if not BLACKLIST_KEYS[k] and t[k] ~= nil and type(v) == type(t[k]) then
            t[k] = v
        end
    end
end

function ConfigManager:save(t)
    if not writefile then return end
    if self.saveTask then task.cancel(self.saveTask) end
    self.saveTask = task.delay(1, function()
        local snap = {}
        for k, v in pairs(t) do
            if not BLACKLIST_KEYS[k] then snap[k] = v end
        end
        local ok, json = pcall(function() return HttpService:JSONEncode(snap) end)
        if ok then pcall(writefile, CONFIG_FILE, json) end
        self.saveTask = nil
    end)
end

ConfigManager:load()

----------------------------------------------------------------
-- STATE
----------------------------------------------------------------
local State = {
    -- Highlight ESP (per role)
    hlInnocent = false, hlMurderer = false, hlSheriff = false,
    -- Box ESP (per role)
    boxInnocent = false, boxMurderer = false, boxSheriff = false,
    -- Tracer ESP (per role)
    tracerInnocent = false, tracerMurderer = false, tracerSheriff = false,
    -- Object ESP
    objGunDrop = false, objKnife = false,
    -- Info
    showDistance = false, showNickname = false,
    roundTimer = false, roleReveal = false,
    crosshair = false, hideSelfESP = false,
    -- Combat
    aimlockTarget = nil, aimlockActive = false,
    sheriffAimlock = false, murdererAimlock = false,
    silentThrowAim = false, autoKnifeThrow = false,
    spawnKnifeAtPlayer = false, murdererWallCheck = true,
    knifeFOVEnabled = false, knifeFOV = 200, knifeOffset = 3.5,
    killAuraOn = false,
    knifePrioritizePing = false, knifePredictJump = false, knifePredictLag = false,
    knifeMaxSim = 80, knifeInterval = 100, knifeHMul = 100, knifeVMul = 100,
    knifeOffsetX = 0, knifeOffsetY = 0, knifeOffsetZ = 0, offsetPingMult = 1.0,
    autoShoot = false, sheriffWallCheck = true, autoUnequipGun = true,
    sheriffCameraTurn = false, autoGetGun = false, shootOffset = 2.8,
    sheriffPrioritizePing = false, sheriffPredictJump = false, sheriffPredictLag = false,
    sheriffMaxSim = 60, sheriffInterval = 100, sheriffHMul = 100, sheriffVMul = 100,
    sheriffOffsetX = 0, sheriffOffsetY = 0, sheriffOffsetZ = 0,
    -- Movement
    fly = false, flySpeed = 50, infJump = false, infJumpLimit2 = false,
    loopWSFOV = false, ws = 16, fov = 70, noclip = false,
    invisible = false, ctrlClickTP = false,
    -- Fling
    touchFling = false,
    -- Anti
    antiFling = false, antiVoid = false, antiAFK = false,
    -- Settings
    uiSounds = true, streamProof = false, fpsCounter = false,
    hubOpen = true,
}
ConfigManager:apply(State)

----------------------------------------------------------------
-- THEME
----------------------------------------------------------------
local Theme = {
    font = Enum.Font.GothamMedium,
    fontBold = Enum.Font.GothamBold,
    text = Color3.fromRGB(255, 255, 255),
    textDim = Color3.fromRGB(170, 160, 200),
    accent = Color3.fromRGB(140, 90, 255),
    accentAlt = Color3.fromRGB(90, 200, 255),
    accentDeep = Color3.fromRGB(75, 50, 180),
    neon = Color3.fromRGB(200, 150, 255),
    primary = Color3.fromRGB(14, 12, 18),
    secondary = Color3.fromRGB(22, 18, 30),
    danger = Color3.fromRGB(255, 50, 80),
    success = Color3.fromRGB(80, 255, 120),
    bgGrad = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(8, 5, 15)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(16, 10, 24)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 6, 18)),
    }),
    strokeGrad = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(140, 90, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(90, 200, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(75, 50, 180)),
    }),
}

local ROLE_COLORS = {
    Murderer = Color3.fromRGB(255, 50, 80),
    Sheriff = Color3.fromRGB(50, 100, 255),
    Hero = Color3.fromRGB(255, 220, 80),
    Innocent = Color3.fromRGB(80, 255, 120),
}

----------------------------------------------------------------
-- UTILS
----------------------------------------------------------------
local function safeChar() return LocalPlayer.Character end
local function safeHRP()
    local c = safeChar()
    return c and c:FindFirstChild("HumanoidRootPart")
end
local function safeHum()
    local c = safeChar()
    return c and c:FindFirstChildOfClass("Humanoid")
end
local function randomString(len)
    len = len or math.random(10, 20)
    local out = {}
    for i = 1, len do out[i] = string.char(math.random(65, 90)) end
    return table.concat(out)
end
local function lighten(c, amt)
    amt = amt or 0.3
    return Color3.new(math.min(1, c.R + amt), math.min(1, c.G + amt), math.min(1, c.B + amt))
end
local function getMap()
    for _, o in ipairs(workspace:GetChildren()) do
        if o:FindFirstChild("CoinContainer") and o:FindFirstChild("Spawns") then return o end
    end
end
local function setCharTransparency(char, t)
    for _, d in ipairs(char:GetDescendants()) do
        if d:IsA("BasePart") or d:IsA("Decal") then d.Transparency = t end
    end
end

----------------------------------------------------------------
-- ROOT GUI
----------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = randomString()
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 3
do
    local ok = pcall(function()
        local hidden = (gethui and gethui()) or (get_hidden_gui and get_hidden_gui())
        if hidden then ScreenGui.Parent = hidden
        elseif syn and syn.protect_gui then syn.protect_gui(ScreenGui) ScreenGui.Parent = CoreGui
        else ScreenGui.Parent = CoreGui end
    end)
    if not ok then ScreenGui.Parent = CoreGui end
end

local ESPGui = Instance.new("ScreenGui")
ESPGui.Name = randomString()
ESPGui.ResetOnSpawn = false
ESPGui.IgnoreGuiInset = true
ESPGui.DisplayOrder = 2
do
    local ok = pcall(function()
        local hidden = (gethui and gethui()) or (get_hidden_gui and get_hidden_gui())
        if hidden then ESPGui.Parent = hidden else ESPGui.Parent = CoreGui end
    end)
    if not ok then ESPGui.Parent = CoreGui end
end

----------------------------------------------------------------
-- SOUNDS
----------------------------------------------------------------
local soundPart = Instance.new("Part")
soundPart.Parent = workspace
soundPart.Transparency = 1 soundPart.CanCollide = false soundPart.Anchored = true
soundPart.Position = Vector3.new(0, 1000, 0)

local Sounds = {}
function Sounds:init()
    local ids = {"rbxassetid://6383304752", "rbxassetid://6383304752", "rbxassetid://6383304752", "rbxassetid://6383304752"}
    local speeds = {1, 1.2, 0.8, 1.5}
    local vols = {0.3, 0.3, 0.25, 0.2}
    local names = {"click", "toggleOn", "toggleOff", "tab"}
    for i = 1, 4 do
        self[names[i]] = Instance.new("Sound")
        self[names[i]].SoundId = ids[i]
        self[names[i]].Volume = vols[i]
        self[names[i]].PlaybackSpeed = speeds[i]
        self[names[i]].Parent = soundPart
    end
end
function Sounds:play(name)
    if not State.uiSounds then return end
    local s = self[name]
    if s then s:Play() end
end
Sounds:init()

----------------------------------------------------------------
-- NOTIFICATIONS
----------------------------------------------------------------
local NotifContainer = Instance.new("Frame", ScreenGui)
NotifContainer.AnchorPoint = Vector2.new(0.5, 0)
NotifContainer.Position = UDim2.new(0.5, 0, 0, 0)
NotifContainer.Size = UDim2.new(0, 420, 1, 0)
NotifContainer.BackgroundTransparency = 1
local nl = Instance.new("UIListLayout", NotifContainer)
nl.Padding = UDim.new(0, 8)
nl.HorizontalAlignment = Enum.HorizontalAlignment.Center
nl.VerticalAlignment = Enum.VerticalAlignment.Top
nl.SortOrder = Enum.SortOrder.LayoutOrder
Instance.new("UIPadding", NotifContainer).PaddingTop = UDim.new(0, 70)

local function notify(text, color, duration)
    color = color or Theme.accent
    duration = duration or 4
    local n = Instance.new("Frame", NotifContainer)
    n.Size = UDim2.new(0, 420, 0, 50)
    n.BackgroundColor3 = Theme.secondary
    n.BackgroundTransparency = 0.05
    n.BorderSizePixel = 0
    n.ClipsDescendants = true
    Instance.new("UICorner", n).CornerRadius = UDim.new(0, 10)
    local st = Instance.new("UIStroke", n)
    st.Color = color st.Thickness = 1.6 st.Transparency = 0.2
    local g = Instance.new("UIGradient", n)
    g.Color = Theme.bgGrad g.Rotation = 45
    local ic = Instance.new("Frame", n)
    ic.AnchorPoint = Vector2.new(0, 0.5)
    ic.Position = UDim2.new(0, 14, 0.5, 0)
    ic.Size = UDim2.fromOffset(8, 24)
    ic.BackgroundColor3 = color ic.BorderSizePixel = 0
    Instance.new("UICorner", ic).CornerRadius = UDim.new(1, 0)
    local l = Instance.new("TextLabel", n)
    l.AnchorPoint = Vector2.new(0, 0.5)
    l.Position = UDim2.new(0, 36, 0.5, 0)
    l.Size = UDim2.new(1, -70, 1, -10)
    l.BackgroundTransparency = 1
    l.Font = Theme.font l.TextColor3 = Theme.text
    l.TextSize = 14 l.TextWrapped = true
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Text = tostring(text)
    local c = Instance.new("TextButton", n)
    c.AnchorPoint = Vector2.new(1, 0.5)
    c.Position = UDim2.new(1, -10, 0.5, 0)
    c.Size = UDim2.fromOffset(22, 22)
    c.BackgroundTransparency = 1 c.Text = "x"
    c.TextColor3 = Theme.text c.Font = Enum.Font.GothamBold c.TextSize = 16
    local sc = Instance.new("UIScale", n)
    sc.Scale = 0.5
    n.Position = UDim2.new(0.5, 0, -0.2, 0)
    TweenService:Create(n, TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(0, 0, 0, 0)
    }):Play()
    TweenService:Create(sc, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Scale = 1
    }):Play()
    local closed = false
    local function closeN()
        if closed then return end
        closed = true
        local f = TweenService:Create(n, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(0.5, 0, -0.2, 0), BackgroundTransparency = 1
        })
        TweenService:Create(sc, TweenInfo.new(0.4), {Scale = 0.5}):Play()
        f:Play()
        f.Completed:Wait()
        n:Destroy()
    end
    c.MouseButton1Click:Connect(closeN)
    task.delay(duration, closeN)
end

----------------------------------------------------------------
-- INPUT
----------------------------------------------------------------
local function safeClick(target, onClick)
    target.Active = true
    local ai = nil
    target.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if ai then return end
            ai = input
            local sc
            sc = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    sc:Disconnect()
                    if ai == input then
                        ai = nil
                        local pos = input.Position
                        local ap = target.AbsolutePosition
                        local as = target.AbsoluteSize
                        if pos.X >= ap.X and pos.X <= ap.X + as.X
                        and pos.Y >= ap.Y and pos.Y <= ap.Y + as.Y then
                            local ok, err = pcall(onClick)
                            if not ok then notify("Error: "..tostring(err), Theme.danger) end
                        end
                    end
                end
            end)
        end
    end)
end

local function makeDraggable(frame, handle)
    handle = handle or frame
    handle.Active = true
    local ai, ds, sp
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if ai then return end
            ai = input ds = input.Position sp = frame.Position
            local sc
            sc = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    sc:Disconnect()
                    if ai == input then ai = nil end
                end
            end)
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if input ~= ai then return end
        if input.UserInputType ~= Enum.UserInputType.MouseMovement
        and input.UserInputType ~= Enum.UserInputType.Touch then return end
        local delta = input.Position - ds
        frame.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y)
    end)
end

----------------------------------------------------------------
-- SPACE BACKGROUND — MAXIMALIST
----------------------------------------------------------------
local function buildSpaceBackground(parent)
    local stars = {}

    local bg = Instance.new("Frame", parent)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(4, 2, 8)
    bg.BorderSizePixel = 0
    bg.ZIndex = 0
    Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 14)

    local bgGrad = Instance.new("UIGradient", bg)
    bgGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(3, 2, 6)),
        ColorSequenceKeypoint.new(0.2, Color3.fromRGB(8, 5, 16)),
        ColorSequenceKeypoint.new(0.4, Color3.fromRGB(4, 3, 10)),
        ColorSequenceKeypoint.new(0.6, Color3.fromRGB(10, 7, 20)),
        ColorSequenceKeypoint.new(0.8, Color3.fromRGB(5, 3, 12)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(12, 8, 24)),
    })
    bgGrad.Rotation = 90

    -- Deep nebula (12 layers)
    local nebColors = {
        Color3.fromRGB(70, 25, 110), Color3.fromRGB(35, 50, 130),
        Color3.fromRGB(110, 35, 90), Color3.fromRGB(50, 15, 80),
        Color3.fromRGB(25, 45, 100), Color3.fromRGB(90, 30, 120),
        Color3.fromRGB(40, 60, 140), Color3.fromRGB(80, 20, 100),
        Color3.fromRGB(30, 40, 110), Color3.fromRGB(100, 40, 130),
        Color3.fromRGB(45, 30, 90), Color3.fromRGB(60, 50, 120),
    }
    for i = 1, 12 do
        local cloud = Instance.new("Frame", bg)
        local sz = math.random(200, 600)
        cloud.Size = UDim2.fromOffset(sz, sz)
        cloud.Position = UDim2.new(math.random(-40, 100) / 100, 0, math.random(-40, 100) / 100, 0)
        cloud.BackgroundColor3 = nebColors[i]
        cloud.BackgroundTransparency = 0.88 + math.random() * 0.05
        cloud.BorderSizePixel = 0
        cloud.ZIndex = 1
        Instance.new("UICorner", cloud).CornerRadius = UDim.new(1, 0)
        local cg = Instance.new("UIGradient", cloud)
        cg.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 0.0),
            NumberSequenceKeypoint.new(0.3, 0.15),
            NumberSequenceKeypoint.new(0.7, 0.5),
            NumberSequenceKeypoint.new(1, 1.0),
        })
        cg.Rotation = math.random(0, 360)
        local sr = cg.Rotation
        local sp = cloud.Position
        task.spawn(function()
            while cloud.Parent do
                TweenService:Create(cloud, TweenInfo.new(math.random(25, 50), Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                    Position = sp + UDim2.new(math.random(-40, 40) / 1000, 0, math.random(-30, 30) / 1000, 0)
                }):Play()
                TweenService:Create(cg, TweenInfo.new(math.random(30, 60), Enum.EasingStyle.Linear), {
                    Rotation = sr + math.random(-50, 50)
                }):Play()
                task.wait(math.random(25, 50))
            end
        end)
    end

    -- Star field — 7 layers (250+ stars)
    local function makeStars(count, z, sMin, sMax, tMin, tMax)
        for i = 1, count do
            local star = Instance.new("Frame", bg)
            local sz = math.random(sMin, sMax) / 10
            star.Size = UDim2.fromOffset(sz, sz)
            star.Position = UDim2.new(math.random(0, 1000) / 1000, 0, math.random(0, 1000) / 1000, 0)
            local h = math.random()
            if h < 0.55 then star.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            elseif h < 0.7 then star.BackgroundColor3 = Color3.fromRGB(200, 220, 255)
            elseif h < 0.82 then star.BackgroundColor3 = Color3.fromRGB(255, 240, 200)
            elseif h < 0.92 then star.BackgroundColor3 = Color3.fromRGB(255, 200, 150)
            else star.BackgroundColor3 = Color3.fromRGB(200, 180, 255) end
            star.BackgroundTransparency = math.random(tMin, tMax) / 100
            star.BorderSizePixel = 0
            star.ZIndex = z
            Instance.new("UICorner", star).CornerRadius = UDim.new(1, 0)
            stars[#stars+1] = star
            -- Glow for larger stars
            if sz >= 2.5 then
                local glow = Instance.new("Frame", bg)
                local gsz = sz * (2 + math.random() * 2)
                glow.Size = UDim2.fromOffset(gsz, gsz)
                glow.Position = star.Position
                glow.BackgroundColor3 = star.BackgroundColor3
                glow.BackgroundTransparency = 0.82 + math.random() * 0.08
                glow.BorderSizePixel = 0
                glow.ZIndex = z - 1
                Instance.new("UICorner", glow).CornerRadius = UDim.new(1, 0)
                stars[#stars+1] = glow
                -- Second glow layer
                local glow2 = Instance.new("Frame", bg)
                glow2.Size = UDim2.fromOffset(gsz * 1.8, gsz * 1.8)
                glow2.Position = star.Position
                glow2.BackgroundColor3 = star.BackgroundColor3
                glow2.BackgroundTransparency = 0.95
                glow2.BorderSizePixel = 0
                glow2.ZIndex = z - 2
                Instance.new("UICorner", glow2).CornerRadius = UDim.new(1, 0)
                stars[#stars+1] = glow2
            end
        end
    end
    makeStars(60, 2, 5, 10, 40, 70)
    makeStars(45, 3, 8, 14, 25, 50)
    makeStars(35, 4, 12, 18, 15, 35)
    makeStars(25, 5, 16, 24, 8, 25)
    makeStars(20, 6, 20, 30, 3, 18)
    makeStars(15, 7, 24, 36, 0, 12)
    makeStars(10, 8, 30, 45, 0, 8)

    -- Twinkle
    task.spawn(function()
        while bg.Parent do
            for _ = 1, 15 do
                local idx = math.random(1, #stars)
                local s = stars[idx]
                if s and s.Parent then
                    TweenService:Create(s, TweenInfo.new(math.random(10, 30) / 10, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
                        BackgroundTransparency = math.random(3, 80) / 100
                    }):Play()
                end
            end
            task.wait(0.25)
        end
    end)

    -- SATURN — proper horizontal rings, tilt 26.7, Cassini Division
    pcall(function()
        local viewport = Instance.new("ViewportFrame", bg)
        viewport.Size = UDim2.new(1, 0, 1, 0)
        viewport.BackgroundTransparency = 1
        viewport.ZIndex = 4
        viewport.Ambient = Color3.fromRGB(35, 22, 55)
        viewport.LightDirection = Vector3.new(-1, -0.5, 1)

        local cam = Instance.new("Camera")
        cam.CFrame = CFrame.new(Vector3.new(0, 4, -15), Vector3.new(0, 0, 0))
        viewport.CurrentCamera = cam

        local TILT = math.rad(26.7)

        -- Planet
        local planet = Instance.new("Part")
        planet.Shape = Enum.PartType.Ball
        planet.Size = Vector3.new(4, 4, 4)
        planet.Color = Color3.fromRGB(218, 188, 138)
        planet.Material = Enum.Material.SmoothPlastic
        planet.Position = Vector3.new(0, 0, 0)
        planet.Anchored = true
        planet.Parent = viewport

        -- Atmosphere
        local atmos = Instance.new("Part")
        atmos.Shape = Enum.PartType.Ball
        atmos.Size = Vector3.new(4.5, 4.5, 4.5)
        atmos.Color = Color3.fromRGB(100, 70, 160)
        atmos.Material = Enum.Material.Neon
        atmos.Transparency = 0.88
        atmos.Position = Vector3.new(0, 0, 0)
        atmos.Anchored = true
        atmos.Parent = viewport

        -- Atmospheric bands (thin discs at different Y heights)
        local bandData = {
            {y = 1.5, c = Color3.fromRGB(235, 210, 160)},
            {y = 0.8, c = Color3.fromRGB(195, 165, 115)},
            {y = 0.2, c = Color3.fromRGB(225, 200, 150)},
            {y = -0.5, c = Color3.fromRGB(205, 175, 125)},
            {y = -1.3, c = Color3.fromRGB(230, 205, 155)},
        }
        local bandParts = {}
        for _, bd in ipairs(bandData) do
            local band = Instance.new("Part")
            band.Shape = Enum.PartType.Cylinder
            band.Size = Vector3.new(0.01, 4.1, 4.1)
            band.Color = bd.c
            band.Material = Enum.Material.SmoothPlastic
            band.Anchored = true
            band.Parent = viewport
            band.CFrame = CFrame.new(0, bd.y, 0) * CFrame.Angles(0, 0, math.rad(90))
            bandParts[#bandParts+1] = {part = band, y = bd.y}
        end

        -- Ring system — horizontal + tilted, with Cassini Division
        local ringDefs = {
            -- C Ring (inner, faint)
            {r = 2.3, t = 0.04, c = Color3.fromRGB(170, 150, 110), tr = 0.75},
            {r = 2.5, t = 0.04, c = Color3.fromRGB(185, 165, 125), tr = 0.65},
            -- B Ring (brightest)
            {r = 2.7, t = 0.03, c = Color3.fromRGB(240, 215, 165), tr = 0.08},
            {r = 2.9, t = 0.03, c = Color3.fromRGB(245, 220, 170), tr = 0.05},
            {r = 3.1, t = 0.03, c = Color3.fromRGB(250, 225, 175), tr = 0.03},
            {r = 3.3, t = 0.03, c = Color3.fromRGB(240, 215, 165), tr = 0.06},
            {r = 3.5, t = 0.03, c = Color3.fromRGB(245, 220, 170), tr = 0.1},
            -- Cassini Division (gap — skip 3.7)
            -- A Ring
            {r = 3.9, t = 0.03, c = Color3.fromRGB(225, 200, 150), tr = 0.2},
            {r = 4.1, t = 0.03, c = Color3.fromRGB(220, 195, 145), tr = 0.18},
            {r = 4.3, t = 0.03, c = Color3.fromRGB(215, 190, 140), tr = 0.25},
            {r = 4.5, t = 0.03, c = Color3.fromRGB(210, 185, 135), tr = 0.3},
            -- F Ring (thin, outer)
            {r = 4.8, t = 0.02, c = Color3.fromRGB(200, 175, 125), tr = 0.5},
        }
        local ringParts = {}
        for _, def in ipairs(ringDefs) do
            local ring = Instance.new("Part")
            ring.Shape = Enum.PartType.Cylinder
            ring.Size = Vector3.new(def.t, def.r * 2, def.r * 2)
            ring.Color = def.c
            ring.Material = Enum.Material.Neon
            ring.Transparency = def.tr
            ring.Anchored = true
            ring.Parent = viewport
            ring.CFrame = CFrame.new(0, 0, 0) * CFrame.Angles(TILT, 0, 0) * CFrame.Angles(0, 0, math.rad(90))
            ringParts[#ringParts+1] = ring
        end

        -- Moon
        local moon = Instance.new("Part")
        moon.Shape = Enum.PartType.Ball
        moon.Size = Vector3.new(0.6, 0.6, 0.6)
        moon.Color = Color3.fromRGB(180, 180, 190)
        moon.Material = Enum.Material.Neon
        moon.Position = Vector3.new(8, 2, -2)
        moon.Anchored = true
        moon.Parent = viewport

        -- Distant planets
        local p1 = Instance.new("Part")
        p1.Shape = Enum.PartType.Ball p1.Size = Vector3.new(1.5, 1.5, 1.5)
        p1.Color = Color3.fromRGB(85, 45, 125) p1.Material = Enum.Material.Neon
        p1.Position = Vector3.new(-14, 5, -22) p1.Anchored = true p1.Parent = viewport

        local p2 = Instance.new("Part")
        p2.Shape = Enum.PartType.Ball p2.Size = Vector3.new(1, 1, 1)
        p2.Color = Color3.fromRGB(50, 100, 200) p2.Material = Enum.Material.Neon
        p2.Position = Vector3.new(15, -4, -25) p2.Anchored = true p2.Parent = viewport

        -- Distant galaxy (spiral — cluster of small parts)
        for i = 1, 30 do
            local gp = Instance.new("Part")
            gp.Shape = Enum.PartType.Ball
            local sz = math.random() * 0.08 + 0.02
            gp.Size = Vector3.new(sz, sz, sz)
            local angle = i * 0.4
            local dist = i * 0.3
            gp.Position = Vector3.new(
                math.cos(angle) * dist - 12,
                math.sin(angle) * dist * 0.3 + 8,
                math.sin(angle) * dist - 30
            )
            gp.Color = Color3.fromRGB(200, 180, 255)
            gp.Material = Enum.Material.Neon
            gp.Anchored = true
            gp.Parent = viewport
        end

        -- 3D background stars (80)
        local stars3D = {}
        for i = 1, 80 do
            local star = Instance.new("Part")
            star.Shape = Enum.PartType.Ball
            local sz = math.random() * 0.12 + 0.02
            star.Size = Vector3.new(sz, sz, sz)
            local c = math.random()
            if c < 0.55 then star.Color = Color3.fromRGB(255, 255, 255)
            elseif c < 0.75 then star.Color = Color3.fromRGB(200, 220, 255)
            elseif c < 0.88 then star.Color = Color3.fromRGB(255, 230, 200)
            else star.Color = Color3.fromRGB(255, 200, 150) end
            star.Material = Enum.Material.Neon
            star.Position = Vector3.new(math.random(-40, 40), math.random(-30, 30), math.random(-50, -10))
            star.Anchored = true
            star.Parent = viewport
            stars3D[#stars3D+1] = star
        end

        -- Camera orbit + animation
        local angle = 0
        task.spawn(function()
            while viewport.Parent do
                angle = angle + 0.0012
                local breath = math.sin(angle * 8) * 0.3
                local x = math.cos(angle) * 15
                local z = math.sin(angle) * 15
                local y = 4 + math.sin(angle * 0.3) * 2 + breath
                cam.CFrame = CFrame.new(Vector3.new(x, y, z), Vector3.new(0, 0, 0))
                planet.CFrame = CFrame.Angles(0, angle * 0.3, 0)
                atmos.CFrame = CFrame.Angles(0, angle * 0.3, 0)
                -- Bands rotate with planet
                for _, bd in ipairs(bandParts) do
                    bd.part.CFrame = CFrame.new(0, bd.y, 0) * CFrame.Angles(0, angle * 0.3, 0) * CFrame.Angles(0, 0, math.rad(90))
                end
                -- Rings: spin + tilt + horizontal
                for _, ring in ipairs(ringParts) do
                    ring.CFrame = CFrame.new(0, 0, 0)
                        * CFrame.Angles(0, angle * 0.3, 0)
                        * CFrame.Angles(TILT, 0, 0)
                        * CFrame.Angles(0, 0, math.rad(90))
                end
                moon.Position = Vector3.new(
                    math.cos(angle * 2.5) * 9,
                    math.sin(angle * 2.5) * 2,
                    math.sin(angle * 2.5) * 9
                )
                atmos.Transparency = 0.85 + math.sin(angle * 15) * 0.05
                -- 3D star twinkle
                for i = 1, 5 do
                    local idx = math.random(1, #stars3D)
                    local s = stars3D[idx]
                    if s and s.Parent then s.Transparency = math.random() * 0.7 end
                end
                task.wait(0.016)
            end
        end)
    end)

    -- Shooting stars
    local function spawnShootingStar()
        local star = Instance.new("Frame", bg)
        star.Size = UDim2.fromOffset(120, 2)
        star.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        star.BorderSizePixel = 0 star.ZIndex = 9
        Instance.new("UICorner", star).CornerRadius = UDim.new(1, 0)
        local g = Instance.new("UIGradient", star)
        g.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 1),
            NumberSequenceKeypoint.new(0.5, 0.05),
            NumberSequenceKeypoint.new(1, 0),
        })
        g.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(140, 90, 255)),
        })
        local sx = math.random(-150, 400) / 1000
        local sy = math.random(30, 500) / 1000
        local ex = sx + math.random(250, 600) / 1000
        local ey = sy + math.random(100, 350) / 1000
        star.Position = UDim2.new(sx, 0, sy, 0)
        star.Rotation = math.deg(math.atan2(ey - sy, ex - sx))
        local tw = TweenService:Create(star, TweenInfo.new(0.8, Enum.EasingStyle.Linear), {
            Position = UDim2.new(ex, 0, ey, 0), BackgroundTransparency = 1
        })
        tw:Play()
        tw.Completed:Connect(function() star:Destroy() end)
    end
    task.spawn(function()
        while bg.Parent do
            task.wait(math.random(4, 10))
            if bg.Parent then spawnShootingStar() end
        end
    end)

    -- Vignette
    local v = Instance.new("Frame", parent)
    v.Size = UDim2.new(1, 0, 1, 0)
    v.BackgroundTransparency = 1 v.BorderSizePixel = 0 v.ZIndex = 10
    Instance.new("UICorner", v).CornerRadius = UDim.new(0, 14)
    local vg = Instance.new("UIGradient", v)
    vg.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.35),
        NumberSequenceKeypoint.new(0.7, 0.95),
        NumberSequenceKeypoint.new(1, 0.35),
    })
    vg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
    })
    vg.Rotation = 90

    -- Purple tint
    local t = Instance.new("Frame", parent)
    t.Size = UDim2.new(1, 0, 1, 0)
    t.BackgroundColor3 = Color3.fromRGB(16, 7, 28)
    t.BackgroundTransparency = 0.55
    t.BorderSizePixel = 0 t.ZIndex = 11
    Instance.new("UICorner", t).CornerRadius = UDim.new(0, 14)
    local tg = Instance.new("UIGradient", t)
    tg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 6, 20)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(6, 4, 12)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 12, 30)),
    })
    tg.Rotation = 90
end

----------------------------------------------------------------
-- MENU FRAME
----------------------------------------------------------------
local MENU_W = 580
local MENU_H = 360

local Menu = Instance.new("Frame", ScreenGui)
Menu.AnchorPoint = Vector2.new(0.5, 0)
Menu.Position = UDim2.new(0.5, 0, -0.6, 0)
Menu.Size = UDim2.fromOffset(MENU_W, MENU_H)
Menu.BackgroundColor3 = Theme.primary
Menu.BorderSizePixel = 0
Menu.ClipsDescendants = true
Menu.Active = true

local menuScale = Instance.new("UIScale", Menu)
Instance.new("UICorner", Menu).CornerRadius = UDim.new(0, 14)
pcall(buildSpaceBackground, Menu)

local menuStroke = Instance.new("UIStroke", Menu)
menuStroke.Thickness = 1.5 menuStroke.Color = Color3.new(1, 1, 1)
local msg = Instance.new("UIGradient", menuStroke)
msg.Color = Theme.strokeGrad msg.Rotation = 45
task.spawn(function()
    while msg.Parent do
        TweenService:Create(msg, TweenInfo.new(8, Enum.EasingStyle.Linear), {Rotation = 405}):Play()
        task.wait(8)
        msg.Rotation = 45
    end
end)

-- Header
local Header = Instance.new("Frame", Menu)
Header.Size = UDim2.new(1, 0, 0, 36)
Header.BackgroundTransparency = 1 Header.ZIndex = 15 Header.Active = true

local hl = Instance.new("Frame", Header)
hl.Size = UDim2.new(1, 0, 0, 1)
hl.Position = UDim2.new(0, 0, 1, -1)
hl.BorderSizePixel = 0 hl.ZIndex = 16
local hlg = Instance.new("UIGradient", hl)
hlg.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
    ColorSequenceKeypoint.new(0.3, Theme.accent),
    ColorSequenceKeypoint.new(0.7, Theme.accentAlt),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
})

local Title = Instance.new("TextLabel", Header)
Title.AnchorPoint = Vector2.new(0, 0.5)
Title.Position = UDim2.new(0, 16, 0.5, 0)
Title.Size = UDim2.new(0, 150, 1, 0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "AXIOM"
Title.TextColor3 = Theme.neon
Title.TextSize = 18
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 16
local tg2 = Instance.new("UIGradient", Title)
tg2.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Theme.accent),
    ColorSequenceKeypoint.new(0.5, Theme.accentAlt),
    ColorSequenceKeypoint.new(1, Theme.accent),
})
task.spawn(function()
    while Title.Parent do
        TweenService:Create(tg2, TweenInfo.new(3, Enum.EasingStyle.Linear), {Offset = Vector2.new(1, 0)}):Play()
        task.wait(3)
        tg2.Offset = Vector2.new(-1, 0)
    end
end)

local Ver = Instance.new("TextLabel", Header)
Ver.AnchorPoint = Vector2.new(1, 0.5)
Ver.Position = UDim2.new(1, -16, 0.5, 0)
Ver.Size = UDim2.new(0, 100, 1, 0)
Ver.BackgroundTransparency = 1
Ver.Font = Theme.font Ver.Text = "v2.0"
Ver.TextColor3 = Theme.accentAlt Ver.TextSize = 12
Ver.TextXAlignment = Enum.TextXAlignment.Right Ver.ZIndex = 16

makeDraggable(Menu, Header)

-- Tab bar
local TabBar = Instance.new("Frame", Menu)
TabBar.Position = UDim2.new(0, 0, 0, 36)
TabBar.Size = UDim2.new(0, 120, 1, -36)
TabBar.BackgroundColor3 = Theme.secondary
TabBar.BackgroundTransparency = 0.35
TabBar.BorderSizePixel = 0 TabBar.ZIndex = 15
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 14)

local tl = Instance.new("UIListLayout", TabBar)
tl.Padding = UDim.new(0, 4)
tl.HorizontalAlignment = Enum.HorizontalAlignment.Center
tl.SortOrder = Enum.SortOrder.LayoutOrder
local tp = Instance.new("UIPadding", TabBar)
tp.PaddingTop = UDim.new(0, 8)
tp.PaddingLeft = UDim.new(0, 6) tp.PaddingRight = UDim.new(0, 6)

local tabSep = Instance.new("Frame", Menu)
tabSep.Size = UDim2.new(0, 1, 1, -36)
tabSep.Position = UDim2.new(0, 120, 0, 36)
tabSep.BackgroundColor3 = Theme.accentDeep
tabSep.BackgroundTransparency = 0.6 tabSep.BorderSizePixel = 0 tabSep.ZIndex = 15

local Content = Instance.new("Frame", Menu)
Content.Position = UDim2.new(0, 120, 0, 36)
Content.Size = UDim2.new(1, -120, 1, -36)
Content.BackgroundTransparency = 1 Content.ZIndex = 15

----------------------------------------------------------------
-- TAB SYSTEM
----------------------------------------------------------------
local Pages = {}
local currentPage = nil

local function makeTab(name, order)
    local btn = Instance.new("TextButton", TabBar)
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(28, 24, 38)
    btn.BackgroundTransparency = 0.1
    btn.BorderSizePixel = 0
    btn.Text = string.upper(name)
    btn.Font = Enum.Font.GothamBold
    btn.TextColor3 = Theme.textDim
    btn.TextSize = 12
    btn.LayoutOrder = order
    btn.AutoButtonColor = false btn.Active = true
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

    local ind = Instance.new("Frame", btn)
    ind.AnchorPoint = Vector2.new(0, 0.5)
    ind.Position = UDim2.new(0, 0, 0.5, 0)
    ind.Size = UDim2.fromOffset(3, 0)
    ind.BackgroundColor3 = Theme.accent
    ind.BorderSizePixel = 0 ind.ZIndex = 2
    Instance.new("UICorner", ind).CornerRadius = UDim.new(1, 0)

    local bg = Instance.new("UIGradient", btn)
    bg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 16, 28)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(14, 12, 18)),
    })
    bg.Rotation = 90

    local container = Instance.new("Frame", Content)
    container.Size = UDim2.new(1, -16, 1, -16)
    container.Position = UDim2.new(0, 8, 0, 8)
    container.BackgroundTransparency = 1
    container.Visible = false container.ZIndex = 15

    local left = Instance.new("ScrollingFrame", container)
    left.Size = UDim2.new(0.48, -2, 1, 0)
    left.BackgroundTransparency = 1
    left.ScrollBarThickness = 2 left.ScrollBarImageColor3 = Theme.accent
    left.AutomaticCanvasSize = Enum.AutomaticSize.Y
    left.CanvasSize = UDim2.new(0, 0, 0, 0)
    left.ZIndex = 15
    local ll = Instance.new("UIListLayout", left)
    ll.Padding = UDim.new(0, 6)
    ll.SortOrder = Enum.SortOrder.LayoutOrder
    ll.HorizontalAlignment = Enum.HorizontalAlignment.Center

    local right = Instance.new("ScrollingFrame", container)
    right.Size = UDim2.new(0.48, -2, 1, 0)
    right.Position = UDim2.new(0.52, 0, 0, 0)
    right.BackgroundTransparency = 1
    right.ScrollBarThickness = 2 right.ScrollBarImageColor3 = Theme.accent
    right.AutomaticCanvasSize = Enum.AutomaticSize.Y
    right.CanvasSize = UDim2.new(0, 0, 0, 0)
    right.ZIndex = 15
    local rl = Instance.new("UIListLayout", right)
    rl.Padding = UDim.new(0, 6)
    rl.SortOrder = Enum.SortOrder.LayoutOrder
    rl.HorizontalAlignment = Enum.HorizontalAlignment.Center

    local pd = {button = btn, container = container, left = left, right = right, order = 0, indicator = ind}

    btn.MouseEnter:Connect(function()
        if currentPage ~= name then
            TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(35, 28, 48), BackgroundTransparency = 0.05}):Play()
            TweenService:Create(btn, TweenInfo.new(0.2), {TextColor3 = Theme.text}):Play()
        end
    end)
    btn.MouseLeave:Connect(function()
        if currentPage ~= name then
            TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(28, 24, 38), BackgroundTransparency = 0.1}):Play()
            TweenService:Create(btn, TweenInfo.new(0.2), {TextColor3 = Theme.textDim}):Play()
        end
    end)
    btn.MouseButton1Click:Connect(function()
        Sounds:play("tab")
        for _, p in pairs(Pages) do
            p.container.Visible = false
            TweenService:Create(p.button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(28, 24, 38), BackgroundTransparency = 0.1}):Play()
            TweenService:Create(p.button, TweenInfo.new(0.2), {TextColor3 = Theme.textDim}):Play()
            TweenService:Create(p.indicator, TweenInfo.new(0.2), {Size = UDim2.fromOffset(3, 0)}):Play()
        end
        container.Visible = true
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Theme.accentDeep, BackgroundTransparency = 0}):Play()
        TweenService:Create(btn, TweenInfo.new(0.2), {TextColor3 = Theme.neon}):Play()
        TweenService:Create(ind, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.fromOffset(3, 20)}):Play()
        left.Position = UDim2.new(0, 15, 0, 0)
        right.Position = UDim2.new(0.52, 15, 0, 0)
        TweenService:Create(left, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Position = UDim2.new(0, 0, 0, 0)}):Play()
        TweenService:Create(right, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Position = UDim2.new(0.52, 0, 0, 0)}):Play()
        currentPage = name
    end)

    Pages[name] = pd
    return pd
end

----------------------------------------------------------------
-- FLOATING BUTTONS — 3 shapes
----------------------------------------------------------------
local FloatingLayer = Instance.new("Frame", ScreenGui)
FloatingLayer.Size = UDim2.new(1, 0, 1, 0)
FloatingLayer.BackgroundTransparency = 1
FloatingLayer.ZIndex = 12

local floatingButtons = {}
local floatCount = 0

local function makeFloatingButton(label, callback, shape)
    if floatingButtons[label] then
        floatingButtons[label]:Destroy()
        floatingButtons[label] = nil
        floatCount = floatCount - 1
        return
    end

    local size, corner
    if shape == "circle" then
        size = 32 corner = UDim.new(1, 0)
    elseif shape == "square" then
        size = 44 corner = UDim.new(0, 8)
    else -- rectangle
        sizeX, sizeY = 100, 28 corner = UDim.new(0, 6)
    end

    local btn = Instance.new("TextButton", FloatingLayer)
    if shape == "rectangle" then
        btn.Size = UDim2.fromOffset(sizeX or 100, sizeY or 28)
    else
        btn.Size = UDim2.fromOffset(size, size)
    end
    btn.Position = UDim2.new(1, -50, 0.5, 20 + floatCount * 40)
    btn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    btn.BackgroundTransparency = 1
    btn.BorderSizePixel = 0
    btn.Text = string.upper(string.sub(label, 1, 10))
    btn.Font = Enum.Font.GothamBold
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = shape == "rectangle" and 8 or 7
    btn.AutoButtonColor = false
    btn.ZIndex = 12
    btn.Active = true
    btn.ClipsDescendants = false
    Instance.new("UICorner", btn).CornerRadius = corner or UDim.new(1, 0)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Thickness = 1.5
    stroke.Color = Color3.new(1, 1, 1)
    local sg = Instance.new("UIGradient", stroke)
    sg.Color = Theme.strokeGrad
    sg.Rotation = 45
    task.spawn(function()
        while sg.Parent do
            TweenService:Create(sg, TweenInfo.new(6, Enum.EasingStyle.Linear), {Rotation = 405}):Play()
            task.wait(6)
            sg.Rotation = 45
        end
    end)

    local scale = Instance.new("UIScale", btn)
    scale.Scale = 0
    TweenService:Create(scale, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()

    -- Drag + click
    local ai, ds, sp, moved
    local DRAG_THRESHOLD = 8
    local lpt

    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if ai then return end
            ai = input ds = input.Position sp = btn.Position moved = false
            if input.UserInputType == Enum.UserInputType.Touch then
                lpt = task.delay(1.5, function()
                    if floatingButtons[label] and not moved then
                        floatingButtons[label]:Destroy()
                        floatingButtons[label] = nil
                        floatCount = floatCount - 1
                    end
                end)
            end
            local sc
            sc = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    sc:Disconnect()
                    if lpt then task.cancel(lpt) lpt = nil end
                    if not moved and ai == input then
                        Sounds:play("click")
                        TweenService:Create(scale, TweenInfo.new(0.08), {Scale = 0.9}):Play()
                        task.delay(0.08, function()
                            TweenService:Create(scale, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
                        end)
                        local ok, err = pcall(callback)
                        if not ok then notify("Error: "..tostring(err), Theme.danger) end
                    end
                    ai = nil
                end
            end)
        end
    end)

    UIS.InputChanged:Connect(function(input)
        if input ~= ai then return end
        if input.UserInputType ~= Enum.UserInputType.MouseMovement
        and input.UserInputType ~= Enum.UserInputType.Touch then return end
        local delta = input.Position - ds
        if math.abs(delta.X) > DRAG_THRESHOLD or math.abs(delta.Y) > DRAG_THRESHOLD then
            moved = true
            if lpt then task.cancel(lpt) lpt = nil end
        end
        if moved then
            btn.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y)
        end
    end)

    btn.MouseButton2Click:Connect(function()
        if floatingButtons[label] then
            floatingButtons[label]:Destroy()
            floatingButtons[label] = nil
            floatCount = floatCount - 1
        end
    end)

    floatingButtons[label] = btn
    floatCount = floatCount + 1
end

----------------------------------------------------------------
-- WIDGET LIBRARY
----------------------------------------------------------------
local function styleBox(frame)
    frame.BackgroundColor3 = Theme.secondary
    frame.BorderSizePixel = 0
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
    local g = Instance.new("UIGradient", frame)
    g.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(26, 22, 36)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(16, 13, 22)),
    })
    g.Rotation = 90
    local s = Instance.new("UIStroke", frame)
    s.Color = Theme.accentDeep s.Thickness = 1 s.Transparency = 0.2
    frame.MouseEnter:Connect(function()
        TweenService:Create(s, TweenInfo.new(0.2), {Transparency = 0.05, Color = Theme.accent}):Play()
    end)
    frame.MouseLeave:Connect(function()
        TweenService:Create(s, TweenInfo.new(0.2), {Transparency = 0.2, Color = Theme.accentDeep}):Play()
    end)
    return s, g
end

local function makeToggle(parent, text, default, callback, stateKey)
    local row = Instance.new("Frame", parent)
    row.Size = UDim2.new(1, -12, 0, 26)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Size = UDim2.new(1, -48, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = string.upper(text)
    lbl.TextColor3 = Theme.text
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local track = Instance.new("Frame", row)
    track.AnchorPoint = Vector2.new(1, 0.5)
    track.Position = UDim2.new(1, -4, 0.5, 0)
    track.Size = UDim2.fromOffset(30, 14)
    track.BackgroundColor3 = Theme.primary
    track.BorderSizePixel = 0
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame", track)
    knob.AnchorPoint = Vector2.new(0, 0.5)
    knob.Position = UDim2.new(0, 2, 0.5, 0)
    knob.Size = UDim2.fromOffset(10, 10)
    knob.BackgroundColor3 = Color3.new(0.5, 0.5, 0.6)
    knob.BorderSizePixel = 0
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local state = default or false
    if stateKey and State[stateKey] ~= nil then state = State[stateKey] end

    local function refresh()
        if state then
            TweenService:Create(knob, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(1, -12, 0.5, 0), BackgroundColor3 = Theme.accent
            }):Play()
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = Theme.accentDeep}):Play()
        else
            TweenService:Create(knob, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(0, 2, 0.5, 0), BackgroundColor3 = Color3.new(0.5, 0.5, 0.6)
            }):Play()
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = Theme.primary}):Play()
        end
    end
    refresh()

    if stateKey and State[stateKey] == true then
        task.spawn(function()
            local ok, err = pcall(callback, true)
            if not ok then warn("[AXIOM] toggle init: "..tostring(err)) end
        end)
    end

    local function flip()
        state = not state
        refresh()
        Sounds:play(state and "toggleOn" or "toggleOff")
        if stateKey then State[stateKey] = state end
        local ok, err = pcall(callback, state)
        if not ok then notify("Error: "..tostring(err), Theme.danger) end
        ConfigManager:save(State)
    end

    row.Active = true
    safeClick(row, flip)
    return row
end

-- Shape selector (3 buttons: circle, square, rectangle)
local function makeShapeSelector(parent, onSelect)
    local row = Instance.new("Frame", parent)
    row.Size = UDim2.new(1, -12, 0, 24)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Size = UDim2.new(0, 80, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Theme.font
    lbl.Text = "SELECT SHAPE"
    lbl.TextColor3 = Theme.textDim
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local shapes = {"circle", "square", "rectangle"}
    local labels = {"O", "[]", "==="}
    local sizes = {{20, 20}, {24, 24}, {40, 16}}
    local corners = {UDim.new(1, 0), UDim.new(0, 4), UDim.new(0, 3)}
    local selected = "circle"

    local buttons = {}
    for i = 1, 3 do
        local b = Instance.new("TextButton", row)
        b.AnchorPoint = Vector2.new(0, 0.5)
        b.Position = UDim2.new(0, 90 + (i - 1) * 45, 0.5, 0)
        b.Size = UDim2.fromOffset(sizes[i][1], sizes[i][2])
        b.BackgroundColor3 = Theme.primary
        b.BorderSizePixel = 0
        b.Text = ""
        b.AutoButtonColor = false
        b.Active = true
        Instance.new("UICorner", b).CornerRadius = corners[i]
        local bs = Instance.new("UIStroke", b)
        bs.Color = Theme.accentDeep bs.Thickness = 1 bs.Transparency = 0.3

        if i == 1 then
            bs.Color = Theme.accent
            bs.Transparency = 0
        end

        b.MouseButton1Click:Connect(function()
            Sounds:play("click")
            selected = shapes[i]
            for j = 1, 3 do
                if j == i then
                    TweenService:Create(buttons[j].stroke, TweenInfo.new(0.15), {Color = Theme.accent, Transparency = 0}):Play()
                else
                    TweenService:Create(buttons[j].stroke, TweenInfo.new(0.15), {Color = Theme.accentDeep, Transparency = 0.3}):Play()
                end
            end
            onSelect(shapes[i])
        end)

        buttons[i] = {btn = b, stroke = bs}
    end

    return row, function() return selected end
end

-- FLOAT TOGGLE (show/hide floating button for this function)
local function makeFloatToggle(parent, text, callback)
    local row = Instance.new("Frame", parent)
    row.Size = UDim2.new(1, -12, 0, 26)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Size = UDim2.new(1, -48, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = string.upper(text)
    lbl.TextColor3 = Theme.text
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local track = Instance.new("Frame", row)
    track.AnchorPoint = Vector2.new(1, 0.5)
    track.Position = UDim2.new(1, -4, 0.5, 0)
    track.Size = UDim2.fromOffset(30, 14)
    track.BackgroundColor3 = Theme.primary
    track.BorderSizePixel = 0
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame", track)
    knob.AnchorPoint = Vector2.new(0, 0.5)
    knob.Position = UDim2.new(0, 2, 0.5, 0)
    knob.Size = UDim2.fromOffset(10, 10)
    knob.BackgroundColor3 = Color3.new(0.5, 0.5, 0.6)
    knob.BorderSizePixel = 0
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local floatOn = false
    local function refresh()
        if floatOn then
            TweenService:Create(knob, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(1, -12, 0.5, 0), BackgroundColor3 = Theme.accent
            }):Play()
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = Theme.accentDeep}):Play()
        else
            TweenService:Create(knob, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(0, 2, 0.5, 0), BackgroundColor3 = Color3.new(0.5, 0.5, 0.6)
            }):Play()
            TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = Theme.primary}):Play()
        end
    end

    row.Active = true
    safeClick(row, function()
        floatOn = not floatOn
        refresh()
        Sounds:play(floatOn and "toggleOn" or "toggleOff")
        callback(floatOn)
    end)

    return row
end

-- MAIN WIDGET: addFunction (box with toggle + shape selector + float toggle)
local function addFunction(pageData, name, default, callback, stateKey)
    local box = Instance.new("Frame", pageData.page)
    box.Size = UDim2.new(1, -6, 0, 96)
    pageData.order = pageData.order + 1
    box.LayoutOrder = pageData.order
    box.ZIndex = 15
    styleBox(box)

    -- Header
    local header = Instance.new("TextLabel", box)
    header.Size = UDim2.new(1, -12, 0, 20)
    header.Position = UDim2.new(0, 6, 0, 4)
    header.BackgroundTransparency = 1
    header.Font = Enum.Font.GothamBold
    header.Text = string.upper(name)
    header.TextColor3 = Theme.neon
    header.TextSize = 13
    header.TextXAlignment = Enum.TextXAlignment.Left
    header.ZIndex = 16

    -- Header gradient line
    local hLine = Instance.new("Frame", box)
    hLine.Size = UDim2.new(1, -12, 0, 1)
    hLine.Position = UDim2.new(0, 6, 0, 24)
    hLine.BorderSizePixel = 0 hLine.ZIndex = 16
    local hg = Instance.new("UIGradient", hLine)
    hg.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.3),
        NumberSequenceKeypoint.new(0.5, 0),
        NumberSequenceKeypoint.new(1, 0.3),
    })
    hg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Theme.accent),
        ColorSequenceKeypoint.new(0.5, Theme.accentAlt),
        ColorSequenceKeypoint.new(1, Theme.accent),
    })

    -- Function toggle
    local toggleRow = makeToggle(box, name, default, callback, stateKey)
    toggleRow.Position = UDim2.new(0, 0, 0, 28)

    -- Shape selector
    local currentShape = "circle"
    local shapeRow = makeShapeSelector(box, function(shape)
        currentShape = shape
    end)
    shapeRow.Position = UDim2.new(0, 0, 0, 56)

    -- Float toggle
    local floatRow = makeFloatToggle(box, name .. " button", function(on)
        if on then
            makeFloatingButton(name, callback, currentShape)
        else
            makeFloatingButton(name, callback, currentShape) -- toggles off
        end
    end)
    floatRow.Position = UDim2.new(0, 0, 0, 74)

    return box
end

-- ACTION WIDGET (box with button + shape selector + float toggle)
local function addAction(pageData, name, callback, big)
    local box = Instance.new("Frame", pageData.page)
    box.Size = UDim2.new(1, -6, 0, big and 96 or 96)
    pageData.order = pageData.order + 1
    box.LayoutOrder = pageData.order
    box.ZIndex = 15
    styleBox(box)

    -- Header
    local header = Instance.new("TextLabel", box)
    header.Size = UDim2.new(1, -12, 0, 20)
    header.Position = UDim2.new(0, 6, 0, 4)
    header.BackgroundTransparency = 1
    header.Font = Enum.Font.GothamBold
    header.Text = string.upper(name)
    header.TextColor3 = Theme.neon
    header.TextSize = 13
    header.TextXAlignment = Enum.TextXAlignment.Left
    header.ZIndex = 16

    local hLine = Instance.new("Frame", box)
    hLine.Size = UDim2.new(1, -12, 0, 1)
    hLine.Position = UDim2.new(0, 6, 0, 24)
    hLine.BorderSizePixel = 0 hLine.ZIndex = 16
    local hg = Instance.new("UIGradient", hLine)
    hg.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.3),
        NumberSequenceKeypoint.new(0.5, 0),
        NumberSequenceKeypoint.new(1, 0.3),
    })
    hg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Theme.accent),
        ColorSequenceKeypoint.new(0.5, Theme.accentAlt),
        ColorSequenceKeypoint.new(1, Theme.accent),
    })

    -- Action button
    local btn = Instance.new("TextButton", box)
    btn.Size = UDim2.new(1, -12, 0, 28)
    btn.Position = UDim2.new(0, 6, 0, 28)
    btn.BorderSizePixel = 0
    btn.Text = string.upper(name)
    btn.Font = Enum.Font.GothamBold
    btn.TextColor3 = Theme.text
    btn.TextSize = big and 14 or 12
    btn.AutoButtonColor = false btn.Active = true btn.ZIndex = 16
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    local bs = Instance.new("UIStroke", btn)
    bs.Color = Theme.accentDeep bs.Thickness = 1 bs.Transparency = 0.3
    btn.MouseEnter:Connect(function()
        TweenService:Create(bs, TweenInfo.new(0.2), {Transparency = 0.05, Color = Theme.accent}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(bs, TweenInfo.new(0.2), {Transparency = 0.3, Color = Theme.accentDeep}):Play()
    end)
    local us = Instance.new("UIScale", btn)
    safeClick(btn, function()
        Sounds:play("click")
        TweenService:Create(us, TweenInfo.new(0.08), {Scale = 0.95}):Play()
        task.delay(0.08, function()
            TweenService:Create(us, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
        end)
        callback(btn)
    end)

    -- Shape selector
    local currentShape = "circle"
    local shapeRow = makeShapeSelector(box, function(shape)
        currentShape = shape
    end)
    shapeRow.Position = UDim2.new(0, 0, 0, 60)

    -- Float toggle
    local floatRow = makeFloatToggle(box, name .. " button", function(on)
        if on then
            makeFloatingButton(name, callback, currentShape)
        else
            makeFloatingButton(name, callback, currentShape)
        end
    end)
    floatRow.Position = UDim2.new(0, 0, 0, 78)

    return box
end

-- ESP BOX WIDGET (header + per-role toggles + optional sub-section)
local function addESPBox(pageData, title, toggles, subSection)
    -- toggles = { {label, stateKey, callback}, ... }
    -- subSection = { title, toggles } or nil

    local totalHeight = 28 + #toggles * 28
    if subSection then
        totalHeight = totalHeight + 24 + #subSection.toggles * 28
    end

    local box = Instance.new("Frame", pageData.page)
    box.Size = UDim2.new(1, -6, 0, totalHeight)
    pageData.order = pageData.order + 1
    box.LayoutOrder = pageData.order
    box.ZIndex = 15
    styleBox(box)

    -- Header
    local header = Instance.new("TextLabel", box)
    header.Size = UDim2.new(1, -12, 0, 20)
    header.Position = UDim2.new(0, 6, 0, 4)
    header.BackgroundTransparency = 1
    header.Font = Enum.Font.GothamBold
    header.Text = string.upper(title)
    header.TextColor3 = Theme.neon
    header.TextSize = 13
    header.TextXAlignment = Enum.TextXAlignment.Left
    header.ZIndex = 16

    local hLine = Instance.new("Frame", box)
    hLine.Size = UDim2.new(1, -12, 0, 1)
    hLine.Position = UDim2.new(0, 6, 0, 24)
    hLine.BorderSizePixel = 0 hLine.ZIndex = 16
    local hg = Instance.new("UIGradient", hLine)
    hg.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.3),
        NumberSequenceKeypoint.new(0.5, 0),
        NumberSequenceKeypoint.new(1, 0.3),
    })
    hg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Theme.accent),
        ColorSequenceKeypoint.new(0.5, Theme.accentAlt),
        ColorSequenceKeypoint.new(1, Theme.accent),
    })

    -- Toggles
    local yOffset = 28
    for _, t in ipairs(toggles) do
        local toggle = makeToggle(box, t.label, false, t.callback, t.stateKey)
        toggle.Position = UDim2.new(0, 0, 0, yOffset)
        yOffset = yOffset + 28
    end

    -- Sub-section (for Object ESP)
    if subSection then
        local subHeader = Instance.new("TextLabel", box)
        subHeader.Size = UDim2.new(1, -12, 0, 18)
        subHeader.Position = UDim2.new(0, 6, 0, yOffset)
        subHeader.BackgroundTransparency = 1
        subHeader.Font = Enum.Font.GothamBold
        subHeader.Text = string.upper(subSection.title)
        subHeader.TextColor3 = Theme.accentAlt
        subHeader.TextSize = 11
        subHeader.TextXAlignment = Enum.TextXAlignment.Left
        subHeader.ZIndex = 16
        yOffset = yOffset + 22

        for _, t in ipairs(subSection.toggles) do
            local toggle = makeToggle(box, t.label, false, t.callback, t.stateKey)
            toggle.Position = UDim2.new(0, 0, 0, yOffset)
            yOffset = yOffset + 28
        end
    end

    return box
end

-- Simple slider widget
local function addSlider(pageData, text, min, max, default, step, callback, stateKey)
    local box = Instance.new("Frame", pageData.page)
    box.Size = UDim2.new(1, -6, 0, 48)
    pageData.order = pageData.order + 1
    box.LayoutOrder = pageData.order
    box.ZIndex = 15
    styleBox(box)

    local lbl = Instance.new("TextLabel", box)
    lbl.Position = UDim2.new(0, 8, 0, 4)
    lbl.Size = UDim2.new(1, -68, 0, 16)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = string.upper(text)
    lbl.TextColor3 = Theme.neon
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.ZIndex = 16

    local valueBox = Instance.new("TextBox", box)
    valueBox.AnchorPoint = Vector2.new(1, 0)
    valueBox.Position = UDim2.new(1, -8, 0, 3)
    valueBox.Size = UDim2.fromOffset(50, 18)
    valueBox.BackgroundColor3 = Theme.primary
    valueBox.BorderSizePixel = 0
    valueBox.Font = Enum.Font.GothamMedium
    valueBox.TextColor3 = Theme.accentAlt
    valueBox.TextSize = 11
    valueBox.ClearTextOnFocus = true
    valueBox.ZIndex = 16
    Instance.new("UICorner", valueBox).CornerRadius = UDim.new(0, 4)
    local vbs = Instance.new("UIStroke", valueBox)
    vbs.Color = Theme.accentDeep vbs.Thickness = 1 vbs.Transparency = 0.3

    local track = Instance.new("Frame", box)
    track.Position = UDim2.new(0, 8, 1, -14)
    track.Size = UDim2.new(1, -16, 0, 5)
    track.BackgroundColor3 = Theme.primary
    track.BorderSizePixel = 0
    track.Active = true track.ZIndex = 16
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local fill = Instance.new("Frame", track)
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Theme.accent fill.BorderSizePixel = 0
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    local fg = Instance.new("UIGradient", fill)
    fg.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Theme.accentDeep),
        ColorSequenceKeypoint.new(0.5, Theme.accent),
        ColorSequenceKeypoint.new(1, Theme.accentAlt),
    })

    local function fmt(v)
        if step and step >= 1 then return tostring(math.floor(v)) else return string.format("%.2f", v) end
    end
    local function applyValue(value, silent)
        value = math.clamp(value, min, max)
        if step and step > 0 then value = math.floor(value / step + 0.5) * step end
        fill.Size = UDim2.new((value - min) / (max - min), 0, 1, 0)
        valueBox.Text = fmt(value)
        if not silent then
            if stateKey then State[stateKey] = value end
            pcall(callback, value)
            ConfigManager:save(State)
        end
    end
    valueBox.Text = fmt(default)
    valueBox.FocusLost:Connect(function()
        local n = tonumber(valueBox.Text)
        if not n then
            local rel = fill.Size.X.Scale
            local cur = min + (max - min) * rel
            if step and step > 0 then cur = math.floor(cur / step + 0.5) * step end
            valueBox.Text = fmt(cur) return
        end
        applyValue(n)
    end)

    local ai, sp, eng, et
    local function ufi(pos)
        local rel = math.clamp((pos.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
        applyValue(min + (max - min) * rel)
    end
    track.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if ai then return end
            ai = input sp = input.Position eng = false
            et = task.delay(0.15, function()
                if ai == input and not eng then eng = true ufi(input.Position) end
            end)
            local sc
            sc = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    sc:Disconnect()
                    if et then task.cancel(et) et = nil end
                    if ai == input then ai = nil end
                    eng = false
                end
            end)
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if input ~= ai then return end
        if input.UserInputType ~= Enum.UserInputType.MouseMovement
        and input.UserInputType ~= Enum.UserInputType.Touch then return end
        if not eng then
            local delta = input.Position - sp
            if math.abs(delta.Y) > math.abs(delta.X) and math.abs(delta.Y) > 8 then
                ai = nil if et then task.cancel(et) end return
            end
            if math.abs(delta.X) > 8 then eng = true if et then task.cancel(et) end end
        end
        if eng then ufi(input.Position) end
    end)
    return box
end

-- Player dropdown
local function addPlayerDropdown(pageData, label, onSelect)
    local box = Instance.new("Frame", pageData.page)
    box.Size = UDim2.new(1, -6, 0, 34)
    pageData.order = pageData.order + 1
    box.LayoutOrder = pageData.order
    box.ZIndex = 15
    styleBox(box)

    local lbl = Instance.new("TextLabel", box)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Size = UDim2.new(0.5, -10, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = string.upper(label)
    lbl.TextColor3 = Theme.neon
    lbl.TextSize = 12
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.ZIndex = 16

    local val = Instance.new("TextLabel", box)
    val.AnchorPoint = Vector2.new(1, 0.5)
    val.Position = UDim2.new(1, -24, 0.5, 0)
    val.Size = UDim2.new(0.5, -24, 1, -4)
    val.BackgroundTransparency = 1
    val.Font = Theme.font
    val.Text = "SELECT..."
    val.TextColor3 = Theme.accentAlt
    val.TextSize = 11
    val.TextXAlignment = Enum.TextXAlignment.Right
    val.ZIndex = 16

    local arrow = Instance.new("TextLabel", box)
    arrow.AnchorPoint = Vector2.new(1, 0.5)
    arrow.Position = UDim2.new(1, -6, 0.5, 0)
    arrow.Size = UDim2.fromOffset(12, 12)
    arrow.BackgroundTransparency = 1
    arrow.Text = "v"
    arrow.Font = Enum.Font.GothamBold
    arrow.TextSize = 10
    arrow.TextColor3 = Theme.accentAlt
    arrow.ZIndex = 16

    local popup = nil
    local function closePopup()
        if popup then popup:Destroy() popup = nil arrow.Text = "v" end
    end
    local function openPopup()
        if popup then closePopup() return end
        Sounds:play("click")
        popup = Instance.new("Frame", ScreenGui)
        popup.ZIndex = 50
        local ap = box.AbsolutePosition
        local as = box.AbsoluteSize
        popup.Position = UDim2.new(0, ap.X, 0, ap.Y + as.Y + 4)
        popup.Size = UDim2.new(0, as.X, 0, math.min(#Players:GetPlayers() * 26, 130))
        popup.BackgroundColor3 = Theme.secondary
        popup.BorderSizePixel = 0 popup.ClipsDescendants = true
        Instance.new("UICorner", popup).CornerRadius = UDim.new(0, 6)
        local ps = Instance.new("UIStroke", popup)
        ps.Color = Theme.accent ps.Thickness = 1
        local scroll = Instance.new("ScrollingFrame", popup)
        scroll.Size = UDim2.new(1, 0, 1, 0)
        scroll.BackgroundTransparency = 1
        scroll.ScrollBarThickness = 2
        scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
        scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
        local sl = Instance.new("UIListLayout", scroll)
        sl.Padding = UDim.new(0, 2)
        for _, p in ipairs(Players:GetPlayers()) do
            local ob = Instance.new("TextButton", scroll)
            ob.Size = UDim2.new(1, 0, 0, 24)
            ob.BackgroundTransparency = 1
            ob.Text = p.DisplayName.." (@"..p.Name..")"
            ob.Font = Theme.font
            ob.TextColor3 = Theme.text
            ob.TextSize = 11
            ob.TextXAlignment = Enum.TextXAlignment.Left
            Instance.new("UIPadding", ob).PaddingLeft = UDim.new(0, 8)
            ob.MouseEnter:Connect(function()
                TweenService:Create(ob, TweenInfo.new(0.15), {BackgroundTransparency = 0.85, TextColor3 = Theme.neon}):Play()
            end)
            ob.MouseLeave:Connect(function()
                TweenService:Create(ob, TweenInfo.new(0.15), {BackgroundTransparency = 1, TextColor3 = Theme.text}):Play()
            end)
            ob.MouseButton1Click:Connect(function()
                Sounds:play("click")
                val.Text = p.DisplayName
                onSelect(p)
                closePopup()
            end)
        end
        arrow.Text = "^"
    end
    safeClick(box, openPopup)
    UIS.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if popup then
                local pos = input.Position
                local ap = box.AbsolutePosition
                local as = box.AbsoluteSize
                local pp = popup.AbsolutePosition
                local ps = popup.AbsoluteSize
                local inF = pos.X >= ap.X and pos.X <= ap.X + as.X and pos.Y >= ap.Y and pos.Y <= ap.Y + as.Y
                local inP = pos.X >= pp.X and pos.X <= pp.X + ps.X and pos.Y >= pp.Y and pos.Y <= pp.Y + ps.Y
                if not inF and not inP then closePopup() end
            end
        end
    end)
    return box
end

local function addText(pageData, text)
    local lbl = Instance.new("TextLabel", pageData.page)
    lbl.Size = UDim2.new(1, -6, 0, 18)
    lbl.AutomaticSize = Enum.AutomaticSize.Y
    lbl.BackgroundTransparency = 1
    lbl.Font = Theme.font
    lbl.Text = text
    lbl.TextColor3 = Theme.textDim
    lbl.TextSize = 11
    lbl.TextWrapped = true
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.RichText = true
    pageData.order = pageData.order + 1
    lbl.LayoutOrder = pageData.order
    lbl.ZIndex = 15
    return lbl
end

----------------------------------------------------------------
-- ESP, ROLE CACHE, PREDICTION, COMBAT, MOVEMENT, FLING, ANTI
----------------------------------------------------------------
-- (Same logic as before — abbreviated for space, full implementations)

local PlayerESP = { highlights = {}, huds = {}, tracers = {}, boxes = {} }

local function createHighlight(player)
    if PlayerESP.highlights[player] then return PlayerESP.highlights[player] end
    if not player.Character then return nil end
    local hl = Instance.new("Highlight")
    hl.Name = randomString()
    hl.Adornee = player.Character
    hl.FillTransparency = 0.55 hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Parent = ESPGui
    PlayerESP.highlights[player] = hl
    return hl
end

local function createHUD(player)
    if PlayerESP.huds[player] then return PlayerESP.huds[player] end
    if not player.Character then return nil end
    local head = player.Character:FindFirstChild("Head")
    if not head then return nil end
    local bb = Instance.new("BillboardGui")
    bb.Name = randomString()
    bb.AlwaysOnTop = true bb.Size = UDim2.fromOffset(200, 38)
    bb.StudsOffset = Vector3.new(0, 2.5, 0) bb.Adornee = head bb.Parent = ESPGui
    local dl = Instance.new("TextLabel", bb)
    dl.Size = UDim2.new(1, 0, 0, 18) dl.BackgroundTransparency = 1
    dl.Font = Enum.Font.GothamBold dl.TextColor3 = Color3.fromRGB(255, 255, 255)
    dl.TextSize = 13 dl.Text = "" dl.TextStrokeTransparency = 0
    local ds = Instance.new("UIStroke", dl) ds.Thickness = 2 ds.Transparency = 0
    local nl2 = Instance.new("TextLabel", bb)
    nl2.Size = UDim2.new(1, 0, 0, 18) nl2.Position = UDim2.new(0, 0, 0, 18)
    nl2.BackgroundTransparency = 1 nl2.Font = Enum.Font.GothamBold
    nl2.TextColor3 = Color3.fromRGB(255, 255, 255) nl2.TextSize = 13
    nl2.Text = player.Name nl2.TextStrokeTransparency = 0
    local ns = Instance.new("UIStroke", nl2) ns.Thickness = 2 ns.Transparency = 0
    PlayerESP.huds[player] = {gui = bb, dist = dl, distStroke = ds, nick = nl2, nickStroke = ns}
    return PlayerESP.huds[player]
end

local function removePlayerESP(player)
    local h = PlayerESP.highlights[player]
    if h then h:Destroy() PlayerESP.highlights[player] = nil end
    local hud = PlayerESP.huds[player]
    if hud and hud.gui then hud.gui:Destroy() PlayerESP.huds[player] = nil end
    if PlayerESP.tracers[player] then
        pcall(function() PlayerESP.tracers[player]:Remove() end)
        PlayerESP.tracers[player] = nil
    end
end

local ObjectESP = { entries = {} }
function ObjectESP:add(adornee, opts)
    if not adornee or not adornee.Parent then return end
    if self.entries[adornee] then self:remove(adornee) end
    opts = opts or {}
    local color = opts.color or Theme.accent
    local entry = {color = color, opts = opts}
    local hl = Instance.new("Highlight")
    hl.Adornee = adornee hl.FillColor = color hl.OutlineColor = color
    hl.FillTransparency = opts.fillTransparency or 0.7 hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop hl.Parent = ESPGui
    entry.highlight = hl
    if opts.label then
        local bb = Instance.new("BillboardGui")
        bb.AlwaysOnTop = true bb.Size = UDim2.fromOffset(120, 30)
        bb.StudsOffset = Vector3.new(0, 2, 0) bb.Adornee = adornee bb.Parent = ESPGui
        local txt = Instance.new("TextLabel", bb)
        txt.Size = UDim2.new(1, 0, 1, 0) txt.BackgroundTransparency = 1
        txt.Font = Enum.Font.GothamBold txt.Text = opts.label
        txt.TextColor3 = color txt.TextSize = 14
        txt.TextStrokeTransparency = 0.4
        entry.label = bb
    end
    self.entries[adornee] = entry
end
function ObjectESP:remove(adornee)
    local e = self.entries[adornee]
    if not e then return end
    if e.highlight then e.highlight:Destroy() end
    if e.label then e.label:Destroy() end
    self.entries[adornee] = nil
end
function ObjectESP:removeGroup(group)
    for adornee, e in pairs(self.entries) do
        if e.opts.group == group then self:remove(adornee) end
    end
end

local FOVCircle = Instance.new("Frame", ESPGui)
FOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
FOVCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVCircle.BackgroundTransparency = 1 FOVCircle.BorderSizePixel = 0
FOVCircle.Size = UDim2.fromOffset(200, 200)
FOVCircle.Visible = false FOVCircle.ZIndex = 1
local fovS = Instance.new("UIStroke", FOVCircle)
fovS.Thickness = 1.2 fovS.Color = Theme.danger fovS.Transparency = 0.3
Instance.new("UICorner", FOVCircle).CornerRadius = UDim.new(1, 0)

local function updateFOVCircle()
    if not State.knifeFOVEnabled then FOVCircle.Visible = false return end
    FOVCircle.Visible = true
    FOVCircle.Size = UDim2.fromOffset(State.knifeFOV * 2, State.knifeFOV * 2)
end

local Crosshair = Instance.new("Frame", ESPGui)
Crosshair.Size = UDim2.fromOffset(20, 20)
Crosshair.AnchorPoint = Vector2.new(0.5, 0.5)
Crosshair.Position = UDim2.new(0.5, 0, 0.5, 0)
Crosshair.BackgroundTransparency = 1
Crosshair.Visible = false Crosshair.ZIndex = 1
do
    local function mk(x, y, w, h)
        local l = Instance.new("Frame", Crosshair)
        l.Position = UDim2.new(0, x, 0, y) l.Size = UDim2.fromOffset(w, h)
        l.BackgroundColor3 = Theme.accentAlt l.BorderSizePixel = 0 l.ZIndex = 2
    end
    mk(2, 9, 5, 2) mk(13, 9, 5, 2) mk(9, 2, 2, 5) mk(9, 13, 2, 5)
end

local roleRevealLabel = Instance.new("TextLabel", ScreenGui)
roleRevealLabel.BackgroundTransparency = 1 roleRevealLabel.Text = ""
roleRevealLabel.Font = Enum.Font.GothamBold roleRevealLabel.TextSize = 24
roleRevealLabel.TextColor3 = Theme.neon
roleRevealLabel.AnchorPoint = Vector2.new(0.5, 0.5)
roleRevealLabel.Position = UDim2.new(0.5, 0, 0.32, 0)
roleRevealLabel.Size = UDim2.fromOffset(400, 40)
roleRevealLabel.TextStrokeTransparency = 0.4 roleRevealLabel.ZIndex = 10

local fpsLabel = Instance.new("TextLabel", ScreenGui)
fpsLabel.BackgroundTransparency = 1 fpsLabel.Text = ""
fpsLabel.Font = Enum.Font.GothamBold fpsLabel.TextSize = 14
fpsLabel.TextColor3 = Theme.accentAlt
fpsLabel.Position = UDim2.new(0, 8, 0, 8)
fpsLabel.Size = UDim2.fromOffset(120, 20)
fpsLabel.TextXAlignment = Enum.TextXAlignment.Left
fpsLabel.ZIndex = 10 fpsLabel.Visible = false

-- ROLE CACHE
local RoleCache = { data = nil, lastUpdate = 0 }
local function refreshRoleCache(force)
    local now = tick()
    if not force and (now - RoleCache.lastUpdate) < 0.4 then return RoleCache.data end
    local rf = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
    if not rf or not rf:IsA("RemoteFunction") then return RoleCache.data end
    local ok, data = pcall(function() return rf:InvokeServer() end)
    if ok and type(data) == "table" then
        RoleCache.data = data RoleCache.lastUpdate = now
    end
    return RoleCache.data
end
task.spawn(function()
    while true do refreshRoleCache(true) task.wait(0.4) end
end)

local function getPlayerRole(player)
    if not RoleCache.data or not player then return "Innocent" end
    local info = RoleCache.data[player.Name]
    return (info and info.Role) or "Innocent"
end

local function isPlayerAlive(player)
    if not RoleCache.data or not player then return true end
    local info = RoleCache.data[player.Name]
    if not info then return true end
    return not info.Killed and not info.Dead
end

local function findMurderer()
    if not RoleCache.data then
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("Knife") then return p end
        end
        if LocalPlayer.Backpack:FindFirstChild("Knife") then return LocalPlayer end
        return nil
    end
    for name, info in pairs(RoleCache.data) do
        if info.Role == "Murderer" and not info.Killed and not info.Dead then
            local p = Players:FindFirstChild(name)
            if p then return p end
        end
    end
    return nil
end

local function findSheriff()
    if not RoleCache.data then
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("Gun") then return p end
        end
        if LocalPlayer.Backpack:FindFirstChild("Gun") then return LocalPlayer end
        return nil
    end
    for name, info in pairs(RoleCache.data) do
        if info.Role == "Sheriff" and not info.Killed and not info.Dead then
            local p = Players:FindFirstChild(name)
            if p then return p end
        end
    end
    for name, info in pairs(RoleCache.data) do
        if info.Role == "Hero" and not info.Killed and not info.Dead then
            local p = Players:FindFirstChild(name)
            if p then return p end
        end
    end
    return nil
end

local function findNearestPlayer()
    local hrp = safeHRP()
    if not hrp then return nil end
    local nearest, shortest = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local other = p.Character:FindFirstChild("HumanoidRootPart")
            if other then
                local d = (hrp.Position - other.Position).Magnitude
                if d < shortest then shortest = d nearest = p end
            end
        end
    end
    return nearest
end

local function getPlayerInKnifeFOV()
    local cam = workspace.CurrentCamera
    if not cam then return nil end
    local vp = cam.ViewportSize
    local center = Vector2.new(vp.X / 2, vp.Y / 2)
    local best, bestDist = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local sp, onScreen = cam:WorldToViewportPoint(hrp.Position)
                if onScreen then
                    local dist = (Vector2.new(sp.X, sp.Y) - center).Magnitude
                    if dist <= State.knifeFOV and dist < bestDist then
                        bestDist = dist best = p
                    end
                end
            end
        end
    end
    return best
end

-- PREDICTION (same as before)
local velocityHistory = {}
local lastPositions = {}
RunService.Heartbeat:Connect(function()
    local now = tick()
    if not (State.knifePredictLag or State.sheriffPredictLag) then return end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local prev = lastPositions[p]
                if prev then
                    local dt = now - prev.t
                    if dt > 0 then
                        local v = (hrp.Position - prev.pos) / dt
                        local hist = velocityHistory[p] or {}
                        table.insert(hist, v)
                        if #hist > 8 then table.remove(hist, 1) end
                        velocityHistory[p] = hist
                    end
                end
                lastPositions[p] = { pos = hrp.Position, t = now }
            end
        end
    end
end)

local function smoothedVelocity(player)
    local hist = velocityHistory[player]
    if not hist or #hist == 0 then
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        return hrp and hrp.AssemblyLinearVelocity or Vector3.zero
    end
    local sum = Vector3.zero
    for _, v in ipairs(hist) do sum = sum + v end
    return sum / #hist
end

local function getPing()
    local ok, ping = pcall(function() return LocalPlayer:GetNetworkPing() * 2 end)
    return ok and ping or 0
end

local function computePredictedPos(player, offset, cfg)
    local char = typeof(player) == "Instance" and player:IsA("Player") and player.Character or player
    if not char then return Vector3.zero end
    local hrp = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then return Vector3.zero end
    if not cfg.prioritizePing and not cfg.predictJump and not cfg.predictLag then
        local vel = hrp.AssemblyLinearVelocity
        local move = hum.MoveDirection
        local predicted = hrp.Position + (vel * Vector3.new(0.75, 0.5, 0.75)) * (offset / 15) + move * offset
        local pf = ((LocalPlayer:GetNetworkPing() * 1000) * ((State.offsetPingMult - 1) * 0.01)) + 1
        return predicted * pf
    end
    local basePos = hrp.Position
    local horizonMs = 0
    if cfg.prioritizePing then horizonMs = getPing() * 1000 end
    horizonMs = math.min(horizonMs, cfg.maxSim)
    local horizonSec = horizonMs / 1000
    local vel
    if cfg.predictLag and typeof(player) == "Instance" and player:IsA("Player") then
        vel = smoothedVelocity(player)
    else
        vel = hrp.AssemblyLinearVelocity
    end
    local stepSec = math.max(1, cfg.interval) / 1000
    local steps = math.max(1, math.floor(horizonSec / stepSec))
    local hMul = cfg.hMul / 100 local vMul = cfg.vMul / 100
    local velXZ = Vector3.new(vel.X, 0, vel.Z) * hMul
    local velY = vel.Y * vMul
    local inAir = hum.FloorMaterial == Enum.Material.Air
    local gravity = workspace.Gravity
    local pos = basePos
    if cfg.predictJump and inAir then
        for _ = 1, steps do
            pos = pos + velXZ * stepSec + Vector3.new(0, velY * stepSec, 0)
            velY = velY - gravity * stepSec
        end
    else
        local totalSec = stepSec * steps
        pos = basePos + Vector3.new(velXZ.X, velY, velXZ.Z) * totalSec
    end
    local cf = hrp.CFrame
    pos = pos + cf.RightVector * ((cfg.offsetX / 100) * 4) + cf.UpVector * ((cfg.offsetY / 100) * 4) + cf.LookVector * ((cfg.offsetZ / 100) * 4)
    return pos
end

local function getSheriffPredicted(target)
    return computePredictedPos(target, State.shootOffset, {
        prioritizePing = State.sheriffPrioritizePing, predictJump = State.sheriffPredictJump,
        predictLag = State.sheriffPredictLag, maxSim = State.sheriffMaxSim,
        interval = State.sheriffInterval, hMul = State.sheriffHMul, vMul = State.sheriffVMul,
        offsetX = State.sheriffOffsetX, offsetY = State.sheriffOffsetY, offsetZ = State.sheriffOffsetZ,
    })
end

local function getKnifePredicted(target)
    return computePredictedPos(target, State.knifeOffset, {
        prioritizePing = State.knifePrioritizePing, predictJump = State.knifePredictJump,
        predictLag = State.knifePredictLag, maxSim = State.knifeMaxSim,
        interval = State.knifeInterval, hMul = State.knifeHMul, vMul = State.knifeVMul,
        offsetX = State.knifeOffsetX, offsetY = State.knifeOffsetY, offsetZ = State.knifeOffsetZ,
    })
end

local function hasLineOfSight(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return false end
    local hrp = safeHRP()
    local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp or not targetHRP then return false end
    local rp = RaycastParams.new()
    rp.FilterType = Enum.RaycastFilterType.Exclude
    rp.FilterDescendantsInstances = {safeChar()}
    local hit = workspace:Raycast(hrp.Position, targetHRP.Position - hrp.Position, rp)
    if not hit then return true end
    return hit.Instance:IsDescendantOf(targetPlayer.Character)
end

-- SILENT AIM
local oldNamecall
local silentAimReady = false
local function setupSilentAim()
    if silentAimReady then return end
    if not hookmetamethod then return end
    local closure = newcclosure or function(f) return f end
    local ok, err = pcall(function()
        oldNamecall = hookmetamethod(game, "__namecall", closure(function(self, ...)
            local method = getnamecallmethod()
            if method == "FireServer" and State.silentThrowAim
               and typeof(self) == "Instance" and self.Name == "KnifeThrown" then
                local args = {...}
                if type(args[1]) == "string" and args[1] == "Throw" and #args >= 3 then
                    local target = nil
                    if State.knifeFOVEnabled then target = getPlayerInKnifeFOV() end
                    if not target then target = findNearestPlayer() end
                    if target and target.Character then
                        local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
                        if tHRP and (not State.murdererWallCheck or hasLineOfSight(target)) then
                            local ok2, predicted = pcall(getKnifePredicted, target)
                            if ok2 and predicted and predicted ~= Vector3.zero then
                                args[3] = CFrame.new(predicted)
                            end
                        end
                    end
                    return oldNamecall(self, table.unpack(args))
                end
            end
            return oldNamecall(self, ...)
        end))
    end)
    if ok then silentAimReady = true else warn("[AXIOM] Silent aim failed: "..tostring(err)) end
end

-- COMBAT
local function shootMurderer(force)
    if findSheriff() ~= LocalPlayer and not force then notify("You are not sheriff.") return end
    local target = findMurderer() or (force and findNearestPlayer())
    if not target or not target.Character then notify("No target.") return end
    if State.sheriffWallCheck and not hasLineOfSight(target) and not force then return end
    local char = safeChar() local hum = safeHum()
    if not char or not hum then return end
    if not char:FindFirstChild("Gun") then
        local g = LocalPlayer.Backpack:FindFirstChild("Gun")
        if g then hum:EquipTool(g) else notify("No gun.") return end
    end
    local predicted = getSheriffPredicted(target)
    local gun = char:WaitForChild("Gun", 1)
    if gun and gun:FindFirstChild("Shoot") then
        local cam = workspace.CurrentCamera local ocf = cam and cam.CFrame or nil
        if State.sheriffCameraTurn and cam then cam.CFrame = CFrame.new(cam.CFrame.Position, predicted) end
        gun.Shoot:FireServer(CFrame.new(char.RightHand.Position), CFrame.new(predicted))
        if State.sheriffCameraTurn and ocf and cam then
            task.delay(0.033, function() if cam and cam.Parent then cam.CFrame = ocf end end)
        end
        if State.autoUnequipGun then
            task.delay(0.03, function() local h = safeHum() if h then h:UnequipTools() end end)
        end
    end
end

local function knifeThrow(silent)
    if findMurderer() ~= LocalPlayer then if not silent then notify("Not murderer.") end return end
    local target
    if State.knifeFOVEnabled then
        target = getPlayerInKnifeFOV()
        if not target then if silent then return end target = findNearestPlayer() end
    else target = findNearestPlayer() end
    if not target or not target.Character then return end
    if State.murdererWallCheck and not hasLineOfSight(target) then return end
    local char = safeChar() local hum = safeHum()
    if not char or not hum then return end
    if not char:FindFirstChild("Knife") then
        local k = LocalPlayer.Backpack:FindFirstChild("Knife")
        if k then hum:EquipTool(k) else return end
    end
    local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
    if not tHRP then return end
    local predicted = getKnifePredicted(target)
    local from = CFrame.new(char.RightHand.Position)
    if State.spawnKnifeAtPlayer then from = CFrame.new(tHRP.Position + (tHRP.CFrame.LookVector * 5)) end
    local knife = char:WaitForChild("Knife", 1)
    local events = knife and knife:FindFirstChild("Events")
    if events and events:FindFirstChild("KnifeThrown") then
        events.KnifeThrown:FireServer("Throw", from, CFrame.new(predicted))
    end
end

-- MOVEMENT
local flyState = { bv = nil, bg = nil, conn = nil, accel = 0 }
local function startFly()
    local hrp = safeHRP() local hum = safeHum()
    if not hrp or not hum then return end
    flyState.bg = Instance.new("BodyGyro", hrp)
    flyState.bg.P = 1e5 flyState.bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    flyState.bg.CFrame = hrp.CFrame
    flyState.bv = Instance.new("BodyVelocity", hrp)
    flyState.bv.P = 1e4 flyState.bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    flyState.bv.Velocity = Vector3.zero
    hum.PlatformStand = true
    flyState.conn = RunService.Heartbeat:Connect(function()
        if not State.fly then return end
        local h = safeHum() local r = safeHRP() local cam = workspace.CurrentCamera
        if not h or not r or not cam then return end
        local move = h.MoveDirection
        if move.Magnitude > 0.01 then flyState.accel = math.min(State.flySpeed, flyState.accel + 2)
        else flyState.accel = math.max(0, flyState.accel - 2) end
        local look = cam.CFrame.LookVector
        local horizontal = Vector3.new(move.X, 0, move.Z)
        if horizontal.Magnitude > 0 then horizontal = horizontal.Unit * flyState.accel end
        flyState.bv.Velocity = horizontal
        flyState.bg.CFrame = CFrame.new(r.Position, r.Position + look)
    end)
end
local function stopFly()
    if flyState.conn then flyState.conn:Disconnect() flyState.conn = nil end
    if flyState.bv then flyState.bv:Destroy() flyState.bv = nil end
    if flyState.bg then flyState.bg:Destroy() flyState.bg = nil end
    local hum = safeHum() if hum then hum.PlatformStand = false end
    flyState.accel = 0
end

local infJumpConn, landedConn
local infJumpDeb, jumpCount, landed = false, 0, true

-- INVISIBLE (seat method)
local INVIS_POS = Vector3.new(-25.95, 84, 3537.55)
local invisChair = nil
local function toggleInvisible()
    if not LocalPlayer.Character then return end
    State.invisible = not State.invisible
    if State.invisible then
        local hrp = safeHRP()
        if not hrp then return end
        local savedPos = hrp.CFrame
        LocalPlayer.Character:MoveTo(INVIS_POS)
        task.wait(0.15)
        local seat = Instance.new("Seat")
        seat.Name = "invischair" seat.Anchored = false seat.CanCollide = false
        seat.Transparency = 1 seat.Position = INVIS_POS seat.Parent = workspace
        local weld = Instance.new("Weld")
        weld.Part0 = seat
        weld.Part1 = LocalPlayer.Character:FindFirstChild("Torso") or LocalPlayer.Character:FindFirstChild("UpperTorso")
        weld.Parent = seat
        task.wait()
        seat.CFrame = savedPos
        setCharTransparency(LocalPlayer.Character, 0.5)
        invisChair = seat
        notify("Invisible: ON", Theme.success, 3)
    else
        if invisChair then invisChair:Destroy() invisChair = nil end
        if LocalPlayer.Character then setCharTransparency(LocalPlayer.Character, 0) end
        notify("Invisible: OFF", Theme.accentAlt, 2)
    end
end

-- FLING
local function microFling(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then notify("No target.") return end
    local char = safeChar() local hum = safeHum() local hrp = safeHRP()
    if not char or not hum or not hrp then return end
    local tHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not tHRP then return end
    local oldPos = hrp.CFrame
    local bv = Instance.new("BodyVelocity", hrp)
    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
    local start = tick()
    repeat
        for i = 1, 8 do
            hrp.CFrame = tHRP.CFrame + Vector3.new(0, (i%2==0) and 1.5 or -1.5, 0)
            hrp.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            hrp.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
            task.wait()
        end
    until tHRP.Velocity.Magnitude > 500 or tick() - start > 2 or not targetPlayer.Character
    bv:Destroy()
    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    repeat
        hrp.CFrame = oldPos * CFrame.new(0, 0.5, 0)
        hum:ChangeState(Enum.HumanoidStateType.GettingUp)
        for _, p in ipairs(char:GetChildren()) do
            if p:IsA("BasePart") then p.Velocity = Vector3.zero p.RotVelocity = Vector3.zero end
        end
        task.wait()
    until (hrp.Position - oldPos.Position).Magnitude < 25
end

-- ANTI
local antiFlingConn, antiFlingMeConn, antiVoidConn
local lastSafePos = Vector3.zero
local detectedFlingers = {}

-- OPEN/CLOSE PLATE
local OpenPlate = Instance.new("TextButton", ScreenGui)
OpenPlate.AnchorPoint = Vector2.new(0, 0.5)
OpenPlate.Position = UDim2.new(0, 14, 0.5, 0)
OpenPlate.Size = UDim2.fromOffset(50, 50)
OpenPlate.BackgroundColor3 = Color3.new(1, 1, 1)
OpenPlate.AutoButtonColor = false OpenPlate.Text = ""
OpenPlate.BorderSizePixel = 0 OpenPlate.ZIndex = 10 OpenPlate.Active = true
Instance.new("UICorner", OpenPlate).CornerRadius = UDim.new(0, 14)
local ps = Instance.new("UIStroke", OpenPlate)
ps.ApplyStrokeMode = Enum.ApplyStrokeMode.Border ps.Thickness = 2.5 ps.Color = Color3.new(1, 1, 1)
local psg = Instance.new("UIGradient", ps) psg.Color = Theme.strokeGrad psg.Rotation = 45
local plateScale = Instance.new("UIScale", OpenPlate)
local Diamond = Instance.new("Frame", OpenPlate)
Diamond.AnchorPoint = Vector2.new(0.5, 0.5)
Diamond.Position = UDim2.new(0.5, 0, 0.5, 0)
Diamond.Size = UDim2.fromOffset(24, 24) Diamond.Rotation = 45
Diamond.BackgroundColor3 = Theme.accent Diamond.BorderSizePixel = 0 Diamond.ZIndex = 11
Instance.new("UICorner", Diamond).CornerRadius = UDim.new(0, 3)
local dg = Instance.new("UIGradient", Diamond)
dg.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Theme.accent),
    ColorSequenceKeypoint.new(0.5, Theme.accentAlt),
    ColorSequenceKeypoint.new(1, Theme.accentDeep),
})
dg.Rotation = 90
local function makeStripe(oY)
    local s = Instance.new("Frame", Diamond)
    s.AnchorPoint = Vector2.new(0.5, 0.5)
    s.Position = UDim2.new(0.5, 0, 0.5, oY)
    s.Size = UDim2.fromOffset(38, 2) s.BackgroundColor3 = Color3.new(1, 1, 1)
    s.BackgroundTransparency = 0.75 s.BorderSizePixel = 0 s.ZIndex = 12
end
makeStripe(-6) makeStripe(0) makeStripe(6)
task.spawn(function()
    while psg.Parent do
        TweenService:Create(psg, TweenInfo.new(6, Enum.EasingStyle.Linear), {Rotation = 405}):Play()
        task.wait(6) psg.Rotation = 45
    end
end)
task.spawn(function()
    while dg.Parent do
        TweenService:Create(dg, TweenInfo.new(4, Enum.EasingStyle.Linear), {Rotation = 450}):Play()
        task.wait(4) dg.Rotation = 90
    end
end)

do
    local ai, ds, sp, moved, pendingClick = nil, nil, nil, false, false
    OpenPlate.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if ai then return end
            ai = input ds = input.Position sp = OpenPlate.Position moved = false
            local sc
            sc = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    sc:Disconnect()
                    if not moved and ai == input then pendingClick = true end
                    ai = nil
                end
            end)
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if input ~= ai then return end
        if input.UserInputType ~= Enum.UserInputType.MouseMovement
        and input.UserInputType ~= Enum.UserInputType.Touch then return end
        local delta = input.Position - ds
        if math.abs(delta.X) > 6 or math.abs(delta.Y) > 6 then moved = true end
        if moved then
            OpenPlate.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y)
        end
    end)

    local function openMenu()
        State.hubOpen = true Menu.Visible = true
        Menu.Position = UDim2.new(0.5, 0, -0.6, 0) menuScale.Scale = 0.7
        TweenService:Create(Menu, TweenInfo.new(1.0, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Position = UDim2.new(0.5, 0, 0.08, 0)
        }):Play()
        TweenService:Create(menuScale, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
        TweenService:Create(Diamond, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Rotation = 225}):Play()
    end
    local function closeMenu()
        State.hubOpen = false
        local exit = TweenService:Create(Menu, TweenInfo.new(0.6, Enum.EasingStyle.Quint, Enum.EasingDirection.In), {
            Position = UDim2.new(0.5, 0, -0.6, 0)
        })
        TweenService:Create(menuScale, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Scale = 0.7}):Play()
        TweenService:Create(Diamond, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Rotation = 45}):Play()
        exit:Play()
        exit.Completed:Connect(function() Menu.Visible = false end)
    end
    RunService.Heartbeat:Connect(function()
        if pendingClick then
            pendingClick = false Sounds:play("click")
            TweenService:Create(plateScale, TweenInfo.new(0.1), {Scale = 0.9}):Play()
            task.delay(0.1, function()
                TweenService:Create(plateScale, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
            end)
            if State.hubOpen then closeMenu() else openMenu() end
        end
    end)
    UIS.InputBegan:Connect(function(inp, proc)
        if proc then return end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) and UIS:IsKeyDown(Enum.KeyCode.LeftAlt) and inp.KeyCode == Enum.KeyCode.Y then
            if State.hubOpen then closeMenu() else openMenu() end
        end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) and UIS:IsKeyDown(Enum.KeyCode.LeftAlt) and inp.KeyCode == Enum.KeyCode.S then
            State.streamProof = not State.streamProof
            ScreenGui.Enabled = not State.streamProof ESPGui.Enabled = not State.streamProof
        end
    end)
end

-- MASTER RENDER LOOP
local fpsCount, fpsTimer = 0, tick()
RunService.RenderStepped:Connect(function()
    fpsCount = fpsCount + 1
    local now = tick()
    if now - fpsTimer >= 1 then
        if State.fpsCounter then fpsLabel.Text = string.format("FPS: %d", fpsCount) fpsLabel.Visible = true
        else fpsLabel.Visible = false end
        fpsCount = 0 fpsTimer = now
    end

    local anyESP = State.hlInnocent or State.hlMurderer or State.hlSheriff
        or State.boxInnocent or State.boxMurderer or State.boxSheriff
        or State.tracerInnocent or State.tracerMurderer or State.tracerSheriff
        or State.showDistance or State.showNickname

    if not anyESP then
        for p, _ in pairs(PlayerESP.highlights) do removePlayerESP(p) end
    else
        local myHRP = safeHRP()
        local cam = workspace.CurrentCamera
        local vp = cam and cam.ViewportSize or Vector2.new(0, 0)
        for _, player in ipairs(Players:GetPlayers()) do
            if player == LocalPlayer and State.hideSelfESP then removePlayerESP(player) continue end
            if not player.Character then removePlayerESP(player) continue end
            local role = getPlayerRole(player)
            local alive = isPlayerAlive(player)
            local color = ROLE_COLORS[role] or ROLE_COLORS.Innocent
            if not alive then removePlayerESP(player) continue end

            local hlOn = false
            if role == "Innocent" and State.hlInnocent then hlOn = true color = ROLE_COLORS.Innocent
            elseif role == "Murderer" and State.hlMurderer then hlOn = true color = ROLE_COLORS.Murderer
            elseif role == "Sheriff" and State.hlSheriff then hlOn = true color = ROLE_COLORS.Sheriff
            elseif role == "Hero" then hlOn = State.hlSheriff color = ROLE_COLORS.Hero end

            if hlOn then
                local hl = PlayerESP.highlights[player] or createHighlight(player)
                if hl then hl.FillColor = color hl.OutlineColor = lighten(color, 0.3) hl.Adornee = player.Character end
                local needHUD = State.showDistance or State.showNickname
                if needHUD then
                    local hud = PlayerESP.huds[player] or createHUD(player)
                    if hud and hud.gui then
                        local head = player.Character:FindFirstChild("Head")
                        if head then
                            hud.gui.Adornee = head
                            hud.dist.Visible = State.showDistance
                            if State.showDistance then
                                hud.distStroke.Color = color
                                if myHRP then hud.dist.Text = string.format("%dm", math.floor((myHRP.Position - head.Position).Magnitude)) end
                            end
                            hud.nick.Visible = State.showNickname
                            if State.showNickname then hud.nickStroke.Color = color hud.nick.Text = player.DisplayName end
                        end
                    end
                else
                    if PlayerESP.huds[player] then PlayerESP.huds[player].gui:Destroy() PlayerESP.huds[player] = nil end
                end
                -- Tracers
                local tracerOn = false
                if role == "Innocent" and State.tracerInnocent then tracerOn = true
                elseif role == "Murderer" and State.tracerMurderer then tracerOn = true
                elseif role == "Sheriff" and State.tracerSheriff then tracerOn = true
                elseif role == "Hero" and State.tracerSheriff then tracerOn = true end

                if tracerOn and cam and myHRP and Drawing then
                    local tHRP = player.Character:FindFirstChild("HumanoidRootPart")
                    if tHRP then
                        local sp, onScreen = cam:WorldToViewportPoint(tHRP.Position)
                        if onScreen then
                            if not PlayerESP.tracers[player] then PlayerESP.tracers[player] = Drawing.new("Line") end
                            local tracer = PlayerESP.tracers[player]
                            if tracer then
                                tracer.Thickness = 1 tracer.Color = color tracer.Transparency = 1
                                tracer.From = Vector2.new(vp.X / 2, vp.Y)
                                tracer.To = Vector2.new(sp.X, sp.Y) tracer.Visible = true
                            end
                        end
                    end
                else
                    if PlayerESP.tracers[player] then PlayerESP.tracers[player]:Remove() PlayerESP.tracers[player] = nil end
                end
            else
                removePlayerESP(player)
            end
        end
    end

    updateFOVCircle()

    local isSheriff = findSheriff() == LocalPlayer
    local isMurderer = findMurderer() == LocalPlayer
    if isSheriff and State.sheriffAimlock then
        local murd = findMurderer()
        if murd and murd.Character then
            local tHRP = murd.Character:FindFirstChild("HumanoidRootPart")
            local mHRP = safeHRP() local cam = workspace.CurrentCamera
            if tHRP and mHRP and cam then
                cam.CFrame = CFrame.new(cam.CFrame.Position, tHRP.Position)
                mHRP.CFrame = CFrame.new(mHRP.Position, Vector3.new(tHRP.Position.X, mHRP.Position.Y, tHRP.Position.Z))
            end
        end
    end
    if isMurderer and State.murdererAimlock then
        local target
        if State.aimlockActive and State.aimlockTarget and State.aimlockTarget.Character then
            target = State.aimlockTarget
        else target = findNearestPlayer() end
        if target and target.Character then
            local tHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local mHRP = safeHRP() local cam = workspace.CurrentCamera
            if tHRP and mHRP and cam then
                cam.CFrame = CFrame.new(cam.CFrame.Position, tHRP.Position)
                mHRP.CFrame = CFrame.new(mHRP.Position, Vector3.new(tHRP.Position.X, mHRP.Position.Y, tHRP.Position.Z))
            end
        end
    end

    if State.loopWSFOV then
        local cam = workspace.CurrentCamera local hum = safeHum()
        if cam then cam.FieldOfView = State.fov end
        if hum then hum.WalkSpeed = State.ws end
    end

    Crosshair.Visible = State.crosshair

    if State.roleReveal then
        local roles = RoleCache.data
        if roles then
            local myInfo = roles[LocalPlayer.Name]
            if myInfo and myInfo.Role and myInfo.Role ~= "Innocent" then
                roleRevealLabel.Text = "You are: " .. myInfo.Role
                roleRevealLabel.TextColor3 = ROLE_COLORS[myInfo.Role] or Theme.neon
            else roleRevealLabel.Text = "" end
        end
    else roleRevealLabel.Text = "" end
end)

RunService.Stepped:Connect(function()
    if State.noclip then
        local char = safeChar() if not char then return end
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") and p.CanCollide then p.CanCollide = false end
        end
    end
end)

workspace.DescendantAdded:Connect(function(child)
    if State.objGunDrop and child.Name == "GunDrop" then
        ObjectESP:add(child, {group = "gun", color = Color3.fromRGB(255, 240, 80), label = "Dropped Gun"})
        notify("Sheriff's gun dropped.")
        if State.autoGetGun then
            task.wait(0.5)
            local char = safeChar()
            if char and child.Parent then
                local prev = char:GetPivot()
                char:PivotTo(child:GetPivot())
                LocalPlayer.Backpack.ChildAdded:Wait()
                char:PivotTo(prev)
            end
        end
    end
    if State.objKnife and child.Name == "Knife" and child:IsA("BasePart") then
        local inChar = false
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character and child:IsDescendantOf(p.Character) then inChar = true break end
        end
        if not inChar then ObjectESP:add(child, {group = "knife", color = Color3.fromRGB(255, 50, 50), label = "Knife"}) end
    end
end)
workspace.DescendantRemoving:Connect(function(child)
    if child.Name == "GunDrop" then ObjectESP:removeGroup("gun") end
    if child.Name == "Knife" then ObjectESP:removeGroup("knife") end
end)

Players.PlayerRemoving:Connect(function(p)
    removePlayerESP(p)
    if State.aimlockTarget == p then State.aimlockTarget = nil end
end)

local function hookChar(p)
    p.CharacterAdded:Connect(function()
        removePlayerESP(p)
        task.wait(0.3)
        setupSilentAim()
        local hum = safeHum()
        if hum and State.ws ~= 16 then hum.WalkSpeed = State.ws end
        local cam = workspace.CurrentCamera
        if cam and State.fov ~= 70 then cam.FieldOfView = State.fov end
    end)
end
for _, p in ipairs(Players:GetPlayers()) do hookChar(p) end
Players.PlayerAdded:Connect(hookChar)

----------------------------------------------------------------
-- TABS ASSEMBLY
----------------------------------------------------------------

-- ===== MAIN =====
local MainTab = makeTab("Main", 1)
local MainL = {page = MainTab.left, order = 0}
local MainR = {page = MainTab.right, order = 0}

addSlider(MainL, "WalkSpeed", 16, 200, State.ws, 1, function(v) State.ws = v end, "ws")
addSlider(MainL, "FOV", 50, 120, State.fov, 1, function(v) State.fov = v end, "fov")
addFunction(MainL, "Loop WS & FOV", false, function(s) State.loopWSFOV = s end, "loopWSFOV")
addFunction(MainL, "Fly", false, function(s) State.fly = s if s then startFly() else stopFly() end end, "fly")
addSlider(MainL, "Fly Speed", 20, 350, State.flySpeed, 5, function(v) State.flySpeed = v end, "flySpeed")
addFunction(MainL, "Noclip", false, function(s) State.noclip = s end, "noclip")
addFunction(MainL, "Infinite Jump", false, function(s)
    State.infJump = s
    if s then
        local hum = safeHum()
        if hum then
            landedConn = hum.StateChanged:Connect(function(_, new)
                if new == Enum.HumanoidStateType.Landed or new == Enum.HumanoidStateType.Running then
                    landed = true jumpCount = 0
                end
            end)
        end
        infJumpConn = UIS.JumpRequest:Connect(function()
            local h = safeHum() if not h then return end
            if State.infJumpLimit2 and jumpCount >= 2 and not landed then return end
            if not infJumpDeb then
                infJumpDeb = true h:ChangeState(Enum.HumanoidStateType.Jumping)
                jumpCount = jumpCount + 1 landed = false
                task.wait(0.1) infJumpDeb = false
            end
        end)
    else
        if infJumpConn then infJumpConn:Disconnect() infJumpConn = nil end
        if landedConn then landedConn:Disconnect() landedConn = nil end
    end
end, "infJump")
addFunction(MainL, "Limit inf-jump to 2", false, function(s) State.infJumpLimit2 = s jumpCount = 0 end, "infJumpLimit2")
addFunction(MainL, "CTRL+Click TP", false, function(s) State.ctrlClickTP = s end, "ctrlClickTP")

UIS.InputBegan:Connect(function(inp, proc)
    if proc then return end
    if State.ctrlClickTP and UIS:IsKeyDown(Enum.KeyCode.LeftControl)
       and inp.UserInputType == Enum.UserInputType.MouseButton1 then
        local mouse = LocalPlayer:GetMouse()
        if mouse.Hit then
            local mHRP = safeHRP()
            if mHRP then mHRP.CFrame = CFrame.new(mouse.Hit.Position) end
        end
    end
end)

addFunction(MainR, "Invisible", false, function(s)
    State.invisible = s
    if s then toggleInvisible() end
end, "invisible")

addAction(MainR, "Teleport to lobby", function()
    local lobby = workspace:FindFirstChild("Lobby")
    if lobby and lobby:FindFirstChild("Spawns") then
        local sp = lobby.Spawns:FindFirstChildWhichIsA("SpawnLocation")
        if sp then safeChar():MoveTo(sp.Position) end
    end
end)
addAction(MainR, "Teleport to map", function()
    local map = getMap()
    local spawns = map and map:FindFirstChild("Spawns")
    if not spawns then notify("No map.") return end
    local kids = spawns:GetChildren()
    if #kids > 0 then safeChar():MoveTo(kids[math.random(1, #kids)].Position) end
end)

-- ===== VISUALS =====
local VisTab = makeTab("Visuals", 2)
local VisL = {page = VisTab.left, order = 0}
local VisR = {page = VisTab.right, order = 0}

-- Highlight ESP box with per-role toggles + Object ESP sub-section
addESPBox(VisL, "HIGHLIGHT ESP", {
    {label = "Innocent", stateKey = "hlInnocent", callback = function(s) State.hlInnocent = s end},
    {label = "Murderer", stateKey = "hlMurderer", callback = function(s) State.hlMurderer = s end},
    {label = "Sheriff", stateKey = "hlSheriff", callback = function(s) State.hlSheriff = s end},
}, {
    title = "OBJECT",
    toggles = {
        {label = "Dropped gun", stateKey = "objGunDrop", callback = function(s)
            State.objGunDrop = s
            if s then
                local map = getMap()
                local drop = map and map:FindFirstChild("GunDrop")
                if drop then ObjectESP:add(drop, {group = "gun", color = Color3.fromRGB(255, 240, 80), label = "Dropped Gun"}) end
            else ObjectESP:removeGroup("gun") end
        end},
        {label = "Murderer's knife", stateKey = "objKnife", callback = function(s)
            State.objKnife = s
            if s then
                for _, v in ipairs(workspace:GetDescendants()) do
                    if v.Name == "Knife" and v:IsA("BasePart") then
                        local inChar = false
                        for _, p in ipairs(Players:GetPlayers()) do
                            if p.Character and v:IsDescendantOf(p.Character) then inChar = true break end
                        end
                        if not inChar then ObjectESP:add(v, {group = "knife", color = Color3.fromRGB(255, 50, 50), label = "Knife"}) end
                    end
                end
            else ObjectESP:removeGroup("knife") end
        end},
    },
})

-- Tracer ESP box with per-role toggles
addESPBox(VisL, "TRACER ESP", {
    {label = "Innocent", stateKey = "tracerInnocent", callback = function(s) State.tracerInnocent = s end},
    {label = "Murderer", stateKey = "tracerMurderer", callback = function(s) State.tracerMurderer = s end},
    {label = "Sheriff", stateKey = "tracerSheriff", callback = function(s) State.tracerSheriff = s end},
})

addESPBox(VisR, "INFO", {
    {label = "Show distance", stateKey = "showDistance", callback = function(s) State.showDistance = s end},
    {label = "Show nickname", stateKey = "showNickname", callback = function(s) State.showNickname = s end},
    {label = "Hide my ESP", stateKey = "hideSelfESP", callback = function(s) State.hideSelfESP = s end},
    {label = "Crosshair", stateKey = "crosshair", callback = function(s) State.crosshair = s end},
})

addFunction(VisR, "Round timer", false, function(s)
    State.roundTimer = s
    if s then
        local tl = Instance.new("TextLabel", ScreenGui)
        tl.BackgroundTransparency = 1 tl.TextColor3 = Theme.neon
        tl.AnchorPoint = Vector2.new(0.5, 0) tl.Position = UDim2.new(0.5, 0, 0, 80)
        tl.Size = UDim2.fromOffset(220, 28) tl.Font = Enum.Font.GothamBold
        tl.TextSize = 18 tl.TextStrokeTransparency = 0.6 tl.Text = "" tl.ZIndex = 10
        task.spawn(function()
            while State.roundTimer do
                local part = workspace:FindFirstChild("RoundTimerPart")
                if part then
                    local t = part:GetAttribute("Time")
                    if t and t > 0 then tl.Text = string.format("%dm %ds", math.floor(t/60), t%60) else tl.Text = "" end
                else tl.Text = "" end
                task.wait(0.5)
            end
            tl:Destroy()
        end)
    end
end, "roundTimer")

addFunction(VisR, "Role reveal", false, function(s) State.roleReveal = s end, "roleReveal")

addAction(VisR, "Copy murderer name", function()
    local m = findMurderer()
    if not m then notify("No murderer.") return end
    if setclipboard then setclipboard(m.Name) end
    notify("Copied: "..m.Name)
end)
addAction(VisR, "Copy sheriff name", function()
    local s = findSheriff()
    if not s then notify("No sheriff.") return end
    if setclipboard then setclipboard(s.Name) end
    notify("Copied: "..s.Name)
end)

-- ===== COMBAT =====
local CmbTab = makeTab("Combat", 3)
local CmbL = {page = CmbTab.left, order = 0}
local CmbR = {page = CmbTab.right, order = 0}

addPlayerDropdown(CmbL, "Select player:", function(p)
    State.aimlockTarget = p notify("Target: "..p.DisplayName)
end)
addAction(CmbL, "Target Aimlock", function(btn)
    State.aimlockActive = not State.aimlockActive
    btn.Text = State.aimlockActive and "TARGET AIMLOCK: ON" or "TARGET AIMLOCK"
end)
addAction(CmbL, "Murderer Aimlock", function(btn)
    State.murdererAimlock = not State.murdererAimlock
    btn.Text = State.murdererAimlock and "MURDERER AIM: ON" or "MURDERER AIMLOCK"
end)
addAction(CmbL, "Sheriff Aimlock", function(btn)
    State.sheriffAimlock = not State.sheriffAimlock
    btn.Text = State.sheriffAimlock and "SHERIFF AIM: ON" or "SHERIFF AIMLOCK"
end)

addFunction(CmbL, "Throw Silent Aim", false, function(s)
    State.silentThrowAim = s if s then setupSilentAim() notify("Silent aim armed.") end
end, "silentThrowAim")
addFunction(CmbL, "Auto Knife Throw", false, function(s) State.autoKnifeThrow = s end, "autoKnifeThrow")
addFunction(CmbL, "Spawn Knife Near Victim", false, function(s) State.spawnKnifeAtPlayer = s end, "spawnKnifeAtPlayer")
addFunction(CmbL, "Wall Check (Murderer)", true, function(s) State.murdererWallCheck = s end, "murdererWallCheck")

addFunction(CmbL, "FOV Circle", false, function(s) State.knifeFOVEnabled = s updateFOVCircle() end, "knifeFOVEnabled")
addSlider(CmbL, "FOV Radius (px)", 50, 600, State.knifeFOV, 5, function(v) State.knifeFOV = v updateFOVCircle() end, "knifeFOV")

addFunction(CmbL, "Kill Aura", false, function(s) State.killAuraOn = s end, "killAuraOn")
addAction(CmbL, "Kill Nearest", function()
    if findMurderer() ~= LocalPlayer then notify("Not murderer.") return end
    local char = safeChar() local hum = safeHum()
    if not char or not hum then return end
    if not char:FindFirstChild("Knife") then
        local k = LocalPlayer.Backpack:FindFirstChild("Knife")
        if k then hum:EquipTool(k) else return end
    end
    local victim = findNearestPlayer()
    if not victim or not victim.Character then return end
    if State.murdererWallCheck and not hasLineOfSight(victim) then return end
    local vHRP = victim.Character:FindFirstChild("HumanoidRootPart")
    local mHRP = safeHRP()
    if not vHRP or not mHRP then return end
    vHRP.CFrame = mHRP.CFrame + mHRP.CFrame.LookVector * 2
    task.wait(0.05)
    char.Knife.Stab:FireServer("Slash")
end)

addSlider(CmbL, "Knife Max Sim (ms)", 30, 120, State.knifeMaxSim, 1, function(v) State.knifeMaxSim = v end, "knifeMaxSim")
addSlider(CmbL, "Knife Interval (ms)", 1, 800, State.knifeInterval, 1, function(v) State.knifeInterval = v end, "knifeInterval")
addSlider(CmbL, "Knife H Mul (%)", 90, 350, State.knifeHMul, 1, function(v) State.knifeHMul = v end, "knifeHMul")
addSlider(CmbL, "Knife V Mul (%)", 90, 350, State.knifeVMul, 1, function(v) State.knifeVMul = v end, "knifeVMul")

-- Right column: Sheriff
addAction(CmbR, "Shoot Murderer", function() shootMurderer(false) end, true)
addFunction(CmbR, "Auto-Shoot", false, function(s) State.autoShoot = s end, "autoShoot")
addFunction(CmbR, "Wall Check (Sheriff)", true, function(s) State.sheriffWallCheck = s end, "sheriffWallCheck")
addFunction(CmbR, "Auto-Unequip", true, function(s) State.autoUnequipGun = s end, "autoUnequipGun")
addFunction(CmbR, "Camera Turn", false, function(s) State.sheriffCameraTurn = s end, "sheriffCameraTurn")
addFunction(CmbR, "Auto-Grab Gun", false, function(s) State.autoGetGun = s end, "autoGetGun")
addAction(CmbR, "Gun Drop Teleport", function()
    local map = getMap()
    local drop = map and map:FindFirstChild("GunDrop")
    if not drop then notify("No gun on ground.") return end
    local char = safeChar() if not char then return end
    local prev = char:GetPivot()
    char:PivotTo(drop:GetPivot())
    LocalPlayer.Backpack.ChildAdded:Wait()
    char:PivotTo(prev)
end)

addSlider(CmbR, "Sheriff Max Sim (ms)", 30, 90, State.sheriffMaxSim, 1, function(v) State.sheriffMaxSim = v end, "sheriffMaxSim")
addSlider(CmbR, "Sheriff Interval (ms)", 1, 800, State.sheriffInterval, 1, function(v) State.sheriffInterval = v end, "sheriffInterval")
addSlider(CmbR, "Sheriff H Mul (%)", 90, 350, State.sheriffHMul, 1, function(v) State.sheriffHMul = v end, "sheriffHMul")
addSlider(CmbR, "Sheriff V Mul (%)", 90, 350, State.sheriffVMul, 1, function(v) State.sheriffVMul = v end, "sheriffVMul")

-- ===== FLING (merged with Anti) =====
local FlTab = makeTab("Fling", 4)
local FlL = {page = FlTab.left, order = 0}
local FlR = {page = FlTab.right, order = 0}

addAction(FlL, "Fling Sheriff", function() local s = findSheriff() if not s then notify("No sheriff.") return end microFling(s) end)
addAction(FlL, "Fling Murderer", function() local m = findMurderer() if not m then notify("No murderer.") return end microFling(m) end)
addAction(FlL, "Fling Nearest", function() local n = findNearestPlayer() if not n then notify("No target.") return end microFling(n) end)
addAction(FlL, "Fling Everyone", function()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            task.spawn(function() microFling(p) end)
            task.wait(0.3)
        end
    end
end)
addFunction(FlL, "Touch Fling", false, function(s)
    State.touchFling = s
    if s then notify("Touch fling ON.") else notify("Touch fling OFF.") end
end, "touchFling")

addFunction(FlR, "Anti-Fling", false, function(s)
    State.antiFling = s
    if s then
        antiFlingConn = RunService.Heartbeat:Connect(function()
            for _, p in ipairs(Players:GetPlayers()) do
                if p == LocalPlayer or not p.Character then continue end
                local pp = p.Character.PrimaryPart
                if not pp then continue end
                if pp.AssemblyAngularVelocity.Magnitude > 50 or pp.AssemblyLinearVelocity.Magnitude > 100 then
                    if not detectedFlingers[p.Name] then
                        detectedFlingers[p.Name] = true
                        notify("Flinger: "..p.Name, Theme.danger)
                    end
                    for _, part in ipairs(p.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                            part.AssemblyAngularVelocity = Vector3.zero
                            part.AssemblyLinearVelocity = Vector3.zero
                            part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0)
                        end
                    end
                end
            end
        end)
        antiFlingMeConn = RunService.Heartbeat:Connect(function()
            local char = safeChar()
            local pp = char and char.PrimaryPart
            if not pp then return end
            if pp.AssemblyLinearVelocity.Magnitude > 250 or pp.AssemblyAngularVelocity.Magnitude > 250 then
                pp.AssemblyLinearVelocity = Vector3.zero
                pp.AssemblyAngularVelocity = Vector3.zero
                if lastSafePos ~= Vector3.zero then pp.CFrame = CFrame.new(lastSafePos) end
            else lastSafePos = pp.Position end
        end)
    else
        if antiFlingConn then antiFlingConn:Disconnect() antiFlingConn = nil end
        if antiFlingMeConn then antiFlingMeConn:Disconnect() antiFlingMeConn = nil end
        detectedFlingers = {}
    end
end, "antiFling")

addFunction(FlR, "Anti-Void", false, function(s)
    State.antiVoid = s
    if s then
        antiVoidConn = RunService.Heartbeat:Connect(function()
            local hrp = safeHRP() local hum = safeHum()
            if not hrp or not hum then return end
            if hum.FloorMaterial ~= Enum.Material.Air then
                lastSafePos = hrp.Position
            elseif hrp.Position.Y < -30 and lastSafePos ~= Vector3.zero then
                hrp.CFrame = CFrame.new(lastSafePos)
                hrp.AssemblyLinearVelocity = Vector3.zero
                notify("Anti-void: saved.", Theme.success, 2)
            end
        end)
    else
        if antiVoidConn then antiVoidConn:Disconnect() antiVoidConn = nil end
    end
end, "antiVoid")

addFunction(FlR, "Anti-AFK", false, function(s)
    State.antiAFK = s
    if s then
        if getconnections then
            for _, c in ipairs(getconnections(LocalPlayer.Idled)) do
                if c.Disable then c:Disable() elseif c.Disconnect then c:Disconnect() end
            end
        end
        LocalPlayer.Idled:Connect(function()
            if not State.antiAFK then return end
            local vu = game:GetService("VirtualUser")
            vu:CaptureController()
            vu:ClickButton2(Vector2.new())
        end)
        notify("Anti-AFK active.")
    end
end, "antiAFK")

-- ===== SETTINGS =====
local SetTab = makeTab("Settings", 5)
local SetL = {page = SetTab.left, order = 0}
local SetR = {page = SetTab.right, order = 0}

addFunction(SetL, "UI Sounds", true, function(s) State.uiSounds = s end, "uiSounds")
addFunction(SetL, "Stream-Proof", false, function(s)
    State.streamProof = s
    ScreenGui.Enabled = not s ESPGui.Enabled = not s
end, "streamProof")
addFunction(SetL, "FPS Counter", false, function(s) State.fpsCounter = s end, "fpsCounter")

addText(SetR, "Settings auto-save on every change.")
addText(SetR, "Stream-proof hotkey: Ctrl+Alt+S")
addText(SetR, "Menu hotkey: Ctrl+Alt+Y")
addAction(SetR, "Unload AXIOM", function()
    if oldNamecall and silentAimReady then
        pcall(function() hookmetamethod(game, "__namecall", oldNamecall) end)
    end
    State.fly = false stopFly()
    State.noclip = false State.touchFling = false
    State.autoShoot = false State.autoKnifeThrow = false
    State.killAuraOn = false State.antiFling = false State.antiVoid = false
    if antiFlingConn then antiFlingConn:Disconnect() end
    if antiFlingMeConn then antiFlingMeConn:Disconnect() end
    if antiVoidConn then antiVoidConn:Disconnect() end
    if infJumpConn then infJumpConn:Disconnect() end
    if landedConn then landedConn:Disconnect() end
    for _, p in ipairs(Players:GetPlayers()) do removePlayerESP(p) end
    for _, p in pairs(PlayerESP.tracers) do pcall(function() p:Remove() end) end
    if invisChair then invisChair:Destroy() end
    ScreenGui:Destroy()
    ESPGui:Destroy()
    soundPart:Destroy()
end)

-- LOOPS
task.spawn(function()
    while task.wait(0.5) do
        if not State.autoShoot then continue end
        if findSheriff() ~= LocalPlayer then continue end
        local murd = findMurderer()
        if not murd or not murd.Character then continue end
        if State.sheriffWallCheck and not hasLineOfSight(murd) then continue end
        shootMurderer(false)
        task.wait(0.2)
    end
end)

task.spawn(function()
    while task.wait(1.5) do
        if State.autoKnifeThrow then knifeThrow(true) end
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        if not State.killAuraOn then continue end
        if findMurderer() ~= LocalPlayer then continue end
        local char = safeChar()
        if not char or not char:FindFirstChild("Knife") then continue end
        local mHRP = safeHRP()
        if not mHRP then continue end
        for _, p in ipairs(Players:GetPlayers()) do
            if p == LocalPlayer or not p.Character then continue end
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then continue end
            local d = (hrp.Position - mHRP.Position).Magnitude
            if d < 7 then
                hrp.CFrame = mHRP.CFrame + mHRP.CFrame.LookVector * 2
                task.wait(0.05)
                pcall(function() char.Knife.Stab:FireServer("Slash") end)
                task.wait(0.3)
            end
        end
    end
end)

task.spawn(function()
    local toggleDir = 0.1
    while task.wait() do
        if not State.touchFling then continue end
        local hrp = safeHRP()
        if hrp then
            local saved = hrp.Velocity
            hrp.Velocity = saved * 10000 + Vector3.new(0, 10000, 0)
            RunService.RenderStepped:Wait()
            hrp.Velocity = saved
            RunService.Stepped:Wait()
            hrp.Velocity = saved + Vector3.new(0, toggleDir, 0)
            toggleDir = -toggleDir
        end
    end
end)

-- INIT
setupSilentAim()

Pages.Main.button.BackgroundColor3 = Theme.accentDeep
Pages.Main.container.Visible = true
currentPage = "Main"
TweenService:Create(Pages.Main.indicator, TweenInfo.new(0.3), {Size = UDim2.fromOffset(3, 20)}):Play()
TweenService:Create(Pages.Main.button, TweenInfo.new(0.2), {TextColor3 = Theme.neon}):Play()

local hum = safeHum()
if hum and State.ws ~= 16 then hum.WalkSpeed = State.ws end
local cam = workspace.CurrentCamera
if cam and State.fov ~= 70 then cam.FieldOfView = State.fov end

State.hubOpen = true
Menu.Visible = true
Menu.Position = UDim2.new(0.5, 0, -0.6, 0)
menuScale.Scale = 0.7
TweenService:Create(Menu, TweenInfo.new(1.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Position = UDim2.new(0.5, 0, 0.08, 0)
}):Play()
TweenService:Create(menuScale, TweenInfo.new(0.8, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Scale = 1
}):Play()

task.wait(0.5)
notify("AXIOM 2.0 loaded", Theme.accent, 4)
